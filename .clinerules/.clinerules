# Between Coffee 系統 .clinerules

> 引用自: .clinerules/.clinerules
> **最後更新**: 2026年8月10日（更新：c-attract z-index 修復 + 浮動購物車細部 + 第 3 橫幅全幅影片）

## 開發者背景與方法論

### 開發者角色
- **新手開發者** — 使用 **Vibe Coding** 方法（跟著感覺走、邊做邊學）
- **多視角分析** — 從四個不同角色視角分析系統，產出平衡解決方案：
  1. 🛠️ **系統工程師** — 技術架構、程式碼品質、效能
  2. 🎨 **前端工程師** — 使用者體驗、互動流程、響應式設計
  3. 🖌️ **UX/UI 設計師** — 品牌調性、視覺一致性、易用性
  4. 👤 **用戶** — 實際使用情境、痛點、需求
- **目標** — 透過多視角碰撞，找到最適合小咖啡店的平衡方案

### Vibe Coding 原則
- 先讓功能跑起來，再回頭優化
- 小步迭代，每次改進一個小功能
- 遇到問題先搜尋、再問、最後才自己寫
- 保持程式碼簡單可讀，不過度工程化
- 享受開發過程，保持學習心態

### 溝通原則
- **提示模糊時逐一詢問** — 我是新手開發者，如果我的提示過於模糊或不夠具體，請不要猜測，而是逐一詢問我每個不清楚的點，確保我們對任務目標有共同理解。
- **高風險任務先深入評估再制定計劃** — 當任務風險高、複雜度高、或存在太多不確定性時，請先深入精確評估相關源碼來制定計劃，必須提出逐步可獨立驗證的方案。例如：
  - 技術債務、不確定性高的內部方法互相調用複雜度
  - 同一結構內，涉及多個系統和流程對接

## 修復與驗證原則

### 端到端全面修復思維
不只是修復或處理單一問題，要像真人審查者一樣思考端到端的全面修復：

1. **先模擬完整流程** — 深入分析整個流程，在腦中模擬流程運行一次。例如修復下單問題時，要追蹤：前端下單 → 後端邏輯處理 → 隊列 → 後端邏輯處理 → 前端顯示，確保整條鏈路邏輯正確。

2. **檢查類似問題** — 修復一個問題後，檢查系統中是否有其他類似模式或位置出現相同問題，一併全面修復。

3. **多次失敗嘗試不同解法** — 如果修復了多次仍然沒有成功，嘗試不同的解決方案，並向用戶說明其他可行方案。

4. **新手友善** — 用戶是新手開發者，可以舉例說明、產生流程圖等輔助理解。

### 驗證後端改動
不要只憑「編輯成功」就回報 bug 改動已經完成。要像真人審查者一樣驗證：

1. **啟動開發伺服器**，模擬修復後的運行。
2. **檢查後端邏輯**：不能有新增的錯誤或警告。
3. **創建腳本直接執行和驗證**，確保邏輯正確。

### 驗證前端改動
不要只憑「編輯成功」就回報 UI 改動已經完成。要像真人審查者一樣驗證：

1. **啟動開發伺服器**，在瀏覽器中打開被修改的頁面。
2. **直接操作這個改動**。如果是新的控制元件（按鈕、輸入框、切換開關）：
   點擊它、確認狀態有按預期改變。
3. **檢查瀏覽器主控台**：不能有新增的錯誤或警告。
4. **使用 Chrome Devtools**，跑一次效能追蹤，並檢查核心網頁指標（Core Web Vitals）。

### GitHub 推送規則
- **不要自動 push 到 GitHub** — 每個任務完成後，不要自行 push 到 GitHub，直到我通知你。

## 項目概述
- **項目名稱**: Between Coffee 咖啡店外帶網站與訂單製作管理系統
- **技術棧**: Django + PostgreSQL + WebSocket + Bootstrap 5
- **核心業務流程**: 顧客下單 → 支付處理 → 加入製作隊列 → 員工製作 → 訂單就緒 → 顧客取餐
- **Live URL**: https://betweencoffee-delivery-enhance-v1.onrender.com/
- **GitHub**: https://github.com/pythonkei/betweencoffee_delivery_enhance
- **部署方式**: Render（Docker 運行時），使用 Dockerfile 構建，autoDeploy 啟用
- **Celery 狀態**: Render free plan 無 Redis，Celery 已配置 MockCelery 優雅降級，任務同步執行
- **WebSocket**: 使用 Django Channels + InMemoryChannelLayer（無 Redis），ASGI 已禁用 AllowedHostsOriginValidator

## 系統核心價值

```
輕量、高效、個性化咖啡店 — 從小店視角出發為最終目標
```

### 系統主張
- 專注單一地區咖啡店外帶場景，不追求大而全
- 小店的人情味、地域親近感是核心競爭力
- 簡單直覺的下單流程、即時狀態更新
- 只賣咖啡和咖啡豆，非連鎖式店鋪產品
- 不模仿連鎖品牌的複雜系統，保持輕量靈活
- 以單一店鋪需求為基礎，可快速調整

### 店内營運參數
- 咖啡機數量: 2 台
- 製作員工: 2 名
- 每名員工同時製作: 2 杯
- 最大同時製作量: 4 杯
- 商品類型: 咖啡、咖啡豆

## 技術架構

### 整體架構
```
前端層: HTML5 + CSS3 + Bootstrap 5 + JavaScript (ES6+)
  └── WebSocket 三層架構: WebSocketCore → 橋接層 → 業務邏輯層
後端層: Django 4.x + Django Channels + DRF
  ├── eshop/ (核心業務: 商品、訂單、支付、隊列)
  ├── cart/ (購物車管理)
  ├── socialuser/ (用戶認證、會員、忠誠度)
  ├── restaurant/ (餐廳/員工管理)
  └── core/ (共用工具函數)
數據層: PostgreSQL + InMemoryChannelLayer + MockCelery
```

### 技術棧
| 層級 | 技術 | 用途 |
|------|------|------|
| 後端框架 | Django 4.x | Web 框架 |
| 即時通訊 | Django Channels 4.x | WebSocket |
| API | Django REST Framework 3.x | REST API |
| 認證 | django-allauth | 社交登入 |
| 數據庫 | PostgreSQL 13+ | 主要存儲 |
| 緩存/通道 | InMemoryChannelLayer | WebSocket（無 Redis） |
| 異步任務 | MockCelery | 同步降級（無 Redis） |
| 前端框架 | Bootstrap 5.x | 響應式 UI |
| 前端 JS | Vanilla JS ES6+ | 客戶端邏輯 |
| 部署 | Render Docker | 生產環境 |

## 核心業務流程

### 訂單狀態機
```
pending → payment_pending (FPS/現金) → paid → waiting → preparing → ready → completed
```

### 支付流程對照
| 支付方式 | 自動化程度 | 積分獎勵位置 |
|---------|:----------:|:----------:|
| PayPal | ✅ 全自動 | `process_payment_success()` |
| Alipay | ✅ 全自動 | `alipay_callback()` 手動調用 |
| FPS | ⚠️ 半自動 | `confirm_fps_payment()` |
| 現金 | ⚠️ 半自動 | `confirm_cash_payment()` |

### 數據模型核心關係
```
OrderModel → CoffeeItem (M2M), BeanItem (M2M), CoffeeQueue (1:1)
CoffeeQueue → Barista (M2O)
CustomerLoyalty → user (1:1), CustomerCoupon (FK)
Barista → name, is_available, current_workload
```

## 開發規範

### 程式碼風格
- Python: flake8, black, isort (配置在 pyproject.toml)
- JavaScript: ES6+ class 語法
- 提交規範: feat/fix/docs/style/refactor/test/chore

### 分支策略
```
main → 生產環境 | develop → 開發環境 | feature/* → 功能開發 | bugfix/* → 問題修復
```

### 關鍵文件索引

#### 後端核心
| 文件 | 行數 | 重要性 |
|------|:----:|:------:|
| `betweencoffee_delivery/settings.py` | 979 | ⭐⭐⭐⭐⭐ |
| `eshop/models/base.py` | 拆分 | ⭐⭐⭐⭐⭐ |
| `eshop/models/order.py` | 1330 | ⭐⭐⭐⭐⭐ |
| `eshop/order_status_manager.py` | 拆分 | ⭐⭐⭐⭐⭐ |
| `eshop/queue_manager_refactored.py` | 拆分 | ⭐⭐⭐⭐⭐ |
| `eshop/consumers.py` | ~300 | ⭐⭐⭐⭐ |
| `eshop/views/payment_views.py` | ~500 | ⭐⭐⭐⭐ |
| `eshop/views/api_views.py` | ~400 | ⭐⭐⭐⭐ |
| `eshop/serializers.py` | 297 | ⭐⭐⭐⭐ |

#### 前端核心
| 文件 | 行數 | 重要性 |
|------|:----:|:------:|
| `static/js/websocket-core.js` | ~200 | ⭐⭐⭐⭐⭐ |
| `static/js/staff-order-management/base-order-renderer-v2.js` | 1405 | ⭐⭐⭐⭐⭐ |
| `static/css/bc-components.css` | ~1900 | ⭐⭐⭐⭐⭐ |
| `static/css/staff_order_management.css` | 1438 | ⭐⭐⭐⭐ |

## 常見開發任務速查

### 新增選項顯示（如濃度）
需修改 3 個層級：
1. **後端序列化器** (`eshop/serializers.py`) — 在 `serialize_order` 的 items 循環新增 mapping
2. **基礎渲染器** (`base-order-renderer-v2.js`) — 在 `_renderItemOptions()` 新增對應的 `option-item`
3. **子渲染器** — 如果子類有硬編碼選項 HTML（如 `_buildOrderHTML()` 中的 inline 選項），也需要更新
4. **共用工具** (`common-utils.js`) — 如果有 `renderOrderItems()` 也需要更新

### CSS 選項行 class
```css
.bc-options-row {
  display: flex; flex-wrap: wrap; align-items: center; gap: 8px;
  color: var(--bc-text-muted); margin: 0; font-size: 1rem; line-height: 1.6;
}
.bc-options-row .option-item {
  display: inline-flex !important; align-items: center !important;
  gap: 2px; white-space: nowrap;
}
.bc-options-row .ov {
  color: #ffffff !important;
}
```

## 除錯指南

### WebSocket 連接問題
1. 檢查 `betweencoffee_delivery/asgi.py` ASGI 配置
2. 檢查 `settings.py` → CHANNEL_LAYERS
3. 檢查 `eshop/consumers.py` 消費者
4. 檢查 `static/js/websocket-core.js` 前端連接
5. 檢查 `eshop/routing.py` WebSocket 路由

### 支付問題
1. 檢查環境變量 (PAYPAL_CLIENT_ID, ALIPAY_APP_ID 等)
2. 檢查 `eshop/views/payment_views.py`
3. 檢查 `eshop/paypal_utils.py`, `eshop/alipay_utils.py`
4. 檢查 `eshop/order_status_manager.py`

### 渲染器濃度選項不顯示
1. 檢查 `eshop/serializers.py` 是否 mapping `strength_level_cn`
2. 檢查子渲染器是否有硬編碼選項（需逐一搜尋 `cup_level_cn`）
3. 檢查渲染器使用的欄位名（API 回傳 `strength_level`，渲染器需檢查 `item.strength_level_cn || item.strength_level`）

## Phase 3 規劃

```
Phase 3A：Phase 2 收尾（優先級：最高）— ✅ 已完成
├── [x] 修復 URL bug
├── [x] 加上 @staff_api_required 裝飾器
├── [x] cleanup 加 try-catch 保護
├── [x] 執行降級方案測試 / 端到端手動驗證
├── [x] 子標籤頁徽章端到端驗證
├── [x] verify_phase2.py 12/12 測試全部通過

Phase 3B：代碼清理（優先級：高）— ✅ 已完成
├── [x] 評估舊版渲染器是否可移除
├── [x] 統一 order.id / order.order_id 使用
├── [x] 添加 JSDoc 註釋

Phase 3C：效能優化（優先級：中）— ✅ 已完成
├── [x] 分析渲染器效能瓶頸
├── [x] 優化 DOM 操作（DocumentFragment 批量插入）
├── [x] 優化倒數計時（requestAnimationFrame vs setInterval）
├── [x] 減少不必要的重新渲染

Phase 3E：技術債務（優先級：P1）
├── [x] 拆分 models.py（1636行 → 5 個子模組）— ✅ 2026-07-23
├── [x] 拆分 queue_manager_refactored.py（1637行 → 6 個子模組）— ✅ 2026-07-23
├── [x] 拆分 order_status_manager.py（1259行 → 4 個子模組）— ✅ 2026-07-23
├── [x] 增加單元測試覆蓋率（52→93 測試全通過；audit_logger 0→100%、status_display 20→100%、payment_handler 30→90%）— ✅ 2026-07-31
├── [ ] 系統性能監控儀表板
└── [ ] 代碼風格統一（flake8/black/isort）

Phase 3F：基礎設施（優先級：P1）— ✅ 已完成
├── [x] 資料庫遷移至 Supabase（Render PostgreSQL → Supabase）— ✅ 2026-07-24
├── [x] 更新 Django settings 與環境變量

Phase 3D：新功能（優先級：低）
├── [x] 濃度選擇功能顯示（所有前端頁面 + 員工管理頁面）— ✅ 2026-07-27
├── [x] WhatsApp 訂單就緒通知（WhatsApp Cloud API）— ✅ 2026-07-28
│   ├── [x] whatsapp_notifier.py（WhatsApp 發送核心模組）
│   ├── [x] notifications.py + status_changer.py（整合觸發）
│   ├── [x] 香港電話自動補國碼（8 位 → 852）
│   ├── [x] 本機測試 + Render 部署
│   └── [x] 生產環境測試成功 ✅
├── [x] 選項行 bc-options-row 全面改版（CSS + JS + 後端）— ✅ 2026-07-27
├── [x] 產品照片增大 10% — ✅ 2026-07-27
├── [x] 選項文字值白色 — ✅ 2026-07-27
├── [x] 選項組間距 8px — ✅ 2026-07-27
├── [x] 浮動返回頂部按鈕（員工頁面）— ✅ 2026-07-27
├── [x] 四種支付方式顯示（所有頁面）— ✅ 2026-07-27
│   ├── [x] order_history.html（客戶訂單歷史）
│   ├── [x] order_payment_confirmation.html（客戶支付確認）
│   ├── [x] queue-manager.js（員工等待子頁面）
│   ├── [x] payment-pending-renderer-v2.js（員工待確認付款頁面）
│   ├── [x] preparing-orders-renderer-v2.js（員工製作中頁面）
│   ├── [x] ready-orders-renderer-v2.js（員工已就緒頁面）
│   └── [x] completed-orders-renderer-v2.js（員工已完成頁面）
├── [x] CSS 全域清理 Phase A-C（2026-07-30）— ✅
│   ├── [x] Phase A: 刪除 7 個無引用 CSS 檔案（temp.css, core.css, module.css, bootstrap-datepicker.css, colorbox.css, _jquery.timepicker.css, _styles.css）
│   ├── [x] Phase B: 合併 toast.css + payment-methods.css + member-nav.css → bc-components.css
│   ├── [x] Phase C: animate.css 客製化（建立 animate-custom.css 252行替代 3591行）
│   ├── [x] Phase C: style-utilities.css 與 Bootstrap 重複評估（建議保留，等待舊模板淘汰）
│   └── [ ] Phase D: ftco- class 逐步淘汰 — 未來優化（涉及 7 class / 53 處模板）
│       28,776行/31檔案 → 25,052行/22檔案（-13%行數, -29%檔案數）
├── [x] 客戶端訂單狀態卡即時更新修復（10-15s 延遲 → 即時）— ✅ 2026-07-31
│   ├── [x] consumers.py OrderConsumer.order_update 展開 event['data']（status 不再為 None）
│   ├── [x] consumers.py QueueConsumer.order_update 同時發送 queue_update + order_status + payment_status
│   └── [x] test_queue_consumer_order_update.py 單元驗證通過
├── [x] 支付寶 Whitelabel 404 診斷（確認是 sandbox 伺服器 404，非我們系統）— ✅ 2026-07-31
│   ├── [x] Render 環境變數設定 ALIPAY_RETURN_URL/NOTIFY_URL（onrender.com）
│   ├── [x] .env 還原 localhost:8081（本地測試適配）
│   └── [x] 移除 alipay 簽名驗證 double-unquote bug（alipay_utils.py + payment_views.py）
├── [x] 訂單確認頁 Toast 狀態變更靜默化 — ✅ 2026-07-31
│   └── [x] order_status_cards.js showToast 改為僅 console.log（不再 window.toast.info）
├── [x] 首頁 feed 區塊兩層設計（形狀輪廓捲動 + 底部照片固定）— ✅ 2026-08-01
│   ├── [x] 上層 .feed__layer-parts：形狀外部深黑 #0e0e0e（移除 bg_4.jpg）＋反相裁切＋feedScroll 捲動
│   └── [x] 下層 .feed：index_feed_image.png 固定（無動畫）
├── [x] 首頁深金容器 UI 全面調整（landing_v3.html）— ✅ 2026-08-04
│   ├── [x] 深金容器 .bc-welcome-panel：顏色 #774606、max-width 630px、高度經多次縮減約 40%
│   ├── [x] 容器中心垂直對齊 carousel banner 底部（margin-top -55px，響應式 -50/-45px）
│   ├── [x] wakemeup.png 160px + top:-92px 上移，與「上次訂單慣常口味」文字垂直對齊
│   ├── [x] 文字 margin-bottom -30px 上移靠近照片（響應式 -22/-16px）
│   ├── [x] 響應式 ≤768/≤480 兩級漸進縮放（容器、圖片、文字等比縮小）
│   ├── [x] 全部使用 .bc-welcome-* UI 類別（bc-components.css），無內聯 CSS
│   └── [x] 容器寬度 54% 已註解（改為 max-width 630px 由內容撐開）
├── [x] Home Slider 改為左側滾動循環動畫（2026-08-05）— ✅
│   ├── [x] static/js/main.js：移除 fadeOutUpBig/fadeInUp → 預設 translate 水平滑動（向左滾動）
│   ├── [x] smartSpeed: 700（滑動 0.7s）、autoplayTimeout: 7000（每 7 秒）
│   └── [x] loop:true + autoplay:true → 無縫左側循環
├── [x] Carousel 層級修復：gif/SVG 固定底層（2026-08-05）— ✅
│   ├── [x] .img-owl-bg z-index 1→0、.gif-box/.noise-img z-index:0（style-utilities + bc-components）
│   ├── [x] .slider-text z-index:10（資訊+按鈕最頂層）＋ pointer-events
│   └── [x] chrome-devtools 驗證：noiseZ:0, gifZ:0, imgOwlZ:0, textZ:10 ✅
├── [x] bc-welcome-panel 細部：user icon 加大 + 兩列文字模組（2026-08-05）— ✅
│   ├── [x] user icon 46px（font-size 1.8rem），響應式 47/41px
│   ├── [x] .bc-welcome-text 兩列組合模組（gap:2px）、積分校列對齊 padding-left = avatar+gap
│   └── [x] 容器背景改為 #292623
├── [x] bc-welcome-panel 社交登入 + 最後訂單快速下單（2026-08-05 深夜）— ✅
│   ├── [x] 社交登入邏輯：登入後「Hey, 歡迎回來」+社群頭像／登入前「Buddy!」+登入連結（views.py 共用 _build_landing_context/_get_user_avatar）
│   ├── [x] Facebook 頭像修復：Graph API 附加 access_token（extra_data 無 picture + 未帶 token 回傳預設頭像）
│   ├── [x] 最後訂單圖片：wakemeup.png → 最後訂單咖啡/咖啡豆產品圖（_get_last_order_image）
│   ├── [x] 最後訂單圖片包成連結：跳轉 /coffee/{id}/ 或 /bean/{id}/ 並預選杯量/奶量/濃度（_get_last_order_link + coffee.html query 預選 JS）
│   ├── [x] 驗證：LAST ORDER LINK: /coffee/8/?cup_level=Medium&milk_level=Medium&strength_level=Normal ✅
│   ├── [x] UI：兩行文字行距 5px、頭像與文字組水平居中（avatarCenter=infoCenter=758px offset 0）、移除 points 三處縮排
│   └── [x] 圖片位移修正：.bc-welcome-img-link 接管絕對定位（桌面 -92px/160px、平板 -77px/140px、手機 -55px/110px）
├── [x] Best Sell 最強人氣顯示 4 件（2026-08-05）— ✅
│   └── [x] betweencoffee_delivery/views.py LandingNew.hot_coffees [:3]→[:4]
├── [x] bc-welcome-panel 細部：greeting 加大 20% + 行距縮減（2026-08-05 下午）— ✅
│   ├── [x] greeting 1→1.2rem（響應式 0.98/0.9rem）、.bc-welcome-text gap 2→1px
│   └── [x] 檔案：bc-components.css
├── [x] Carousel GIF 響應式放大（2026-08-05 下午）— ✅
│   ├── [x] .gif-box img 70%/80%/90%、.noise-img 55/65/75vw（三級響應）
│   ├── [x] !important 置檔案末尾覆蓋 min-width:238px
│   └── [x] devtools 驗證 768px：noise 422px、gif 537px ✅
├── [x] 首頁 Banner 烘焙影片 + 效能優化 P0/P1 + Render 診斷（2026-08-07）— ✅
│   ├── [x] 咖啡豆烘焙影片放入 owl-carousel 第二個 slider（webm 優先 2.2MB / mp4 fallback）
│   ├── [x] blobsVideo morph 遮罩動畫（border-radius 變形 + morph 7s infinite）
│   ├── [x] 影片 1080×608 置中：margin auto（避免 morph transform 覆蓋）+ 外層滿版深黑
│   ├── [x] P0：Whitenoise 長快取 WHITENOISE_MAX_AGE=31536000（影片下載 4次→1次，-6.6MB）
│   ├── [x] P1：WebSocket fallback 輪詢 5s→30s、baseDelay 1s→5s（-83% 請求）
│   ├── [x] Render 診斷：WebSocketManager 初始化 2 次 = 多 Daphne worker（建議 WEB_CONCURRENCY=1）
│   └── [x] 涉及：landing_v3/index.html、style-utilities.css、settings.py、websocket-core.js
├── [x] home-slider 抖動縮放 + 延遲顯示 + 影片自動播放全面修復（2026-08-07 傍晚）— ✅
│   ├── [x] ⚠️ 重要教訓：WHITENOISE_MAX_AGE=1 年長快取，改 CSS/JS 後必須加 ?v= 版本參數（base.html bc-components.css?v=20260807e / main.js?v=20260807h），否則瀏覽器永遠顯示舊版（反覆「看似無效」的根因）
│   ├── [x] Phase 1 消除抖動：.home-slider/.slider-item 提前鎖定 750px（響應式 560/480px）+ overflow:hidden（.owl-stage 系列全鎖死）+ 深色佔位
│   ├── [x] Phase 2 加速：影片 data-lazy-video preload=none（省 2.2MB）+ 圖片 loading=lazy + base.html extra_head block + 首屏 3 資產 preload
│   ├── [x] 影片自動播放：owl loop 會 clone 複製影片 → 原 `pause 所有影片` 邏輯把剛載入的暫停（currentTime 0.4 被 pause 回 0）→ 移除 pause + 直接 video.play()（muted+playsinline 下真實瀏覽器允許 autoplay）
│   ├── [x] 金色掃光佔位移除：使用者指定不需 shimmer → .gif-box 改透明（保留 img aspect-ratio）
│   ├── [x] GIF 上移不居中：.gif-box(div) 也被設 aspect-ratio → div 高度≠img 高度 → 只保留 <img> 上的 aspect-ratio
│   ├── [x] 教訓：不要用 Pillow 縮小 GIF 原檔存檔（顯示靠 CSS width:50%，原檔 1500px 才是畫質）
│   ├── [x] 實際首頁是 index.html（/ 路由渲染），非 landing_v3.html（/landing/ → LandingNew）
│   └── [x] 涉及：bc-components.css、main.js、base.html、index.html、landing_v3.html
├── [x] 咖啡菜單頁 Z 字對角主副卡（2026-08-06）— ✅
│   ├── [x] 每 4 個一組 2x2 網格（DOM = 大 小 小 大）：左上大、右上小、左下小、右下大
│   ├── [x] 圖片尺寸：大卡 +15%（顯示 290px）、小卡 +10% 再 +5%（260px）
│   ├── [x] 位置落差：cell2/cell3 margin-top 6rem（96px 階梯錯位）
│   ├── [x] 修正拉長：強制 height:auto + min-height:0（覆蓋 style-bootstrap height:36vh）
│   └── [x] chrome-devtools 驗證：8 商品比例 1.00、落差每組 +96px ✅
├── [x] 咖啡描述 CSS 類 .coffee-menu-desc（2026-08-06）— ✅
│   ├── [x] 方案分析：增加內邊距優於減少寬度（不破壞卡片對齊/自然縮短行長/增加呼吸感）
│   ├── [x] padding 1rem + max-width 75% + font-size 1rem + line-height 1.7
│   └── [x] chrome-devtools 驗證：maxWidth:75%、fontSize:16px ✅
├── [x] 全站安全審查與修復 + 支付寶金鑰輪換 — ✅ 2026-08-01
│   ├── [x] 移除 git 追蹤 4 個敏感金鑰 + .gitignore 排除 keys/
│   ├── [x] settings.py 移除明文密碼/API keys/資料庫憑證 + 硬編碼 PayPal 憑證
│   ├── [x] DebugMiddleware 移除（不再記錄個資）
│   ├── [x] 加入 HSTS + Referrer Policy + 強化密碼驗證
│   ├── [x] 支付寶金鑰輪換（新 RSA2 金鑰對 + 控制台加簽變更）
│   ├── [x] settings.py 環境變數優先 read_alipay_key()
│   └── [x] Render 環境變數設定新金鑰（需手動 Deploy latest commit，autoDeploy=off）
├── [x] FPS/Cash 付款頁面 UI 改良 — ✅ 2026-08-01
│   ├── [x] FPS 新增商品明細 + 訂單資訊區域、隱藏參考編號、付款說明全寬
│   ├── [x] Cash 付款說明全寬
│   └── [x] 按鈕統一 calltoaction-btn（主按鈕金色、返回修改灰色）
├── [x] 訂單狀態卡 Icon 全面優化（前後端一致）— ✅ 2026-08-02
│   ├── [x] 已完成：`fa-trophy`/`fa-check-double` → `fa-circle-check`（4 檔案統一）
│   ├── [x] 製作中：`fa-coffee` → `fa-mug-hot`
│   ├── [x] 待取餐：`fa-check-circle` → `fa-bell`
│   └── [x] 最終流程：📋 → ☕ → 🔔 → ✅
├── [x] 優先處理徽章統一 `.badge-expedited` — ✅ 2026-08-01
│   └── [x] 4 JS + staff_order_management.css
├── [x] WhatsApp Token 診斷與更新 — ✅ 2026-08-01
│   ├── [x] Token 過期診斷（test_whatsapp_diagnosis.py）
│   ├── [x] 更新 .env（驗證 HTTP 200）
│   └── [x] ⚠️ Render 環境變數待更新
├── [x] 全站行動響應式修復 — ✅ 2026-08-03
│   ├── [x] 支付確認頁 Y 軸滾動條（.svg-bg-4，還原原始結構）
│   ├── [x] 行動導航背景透明化 + 漢堡圖標白色
│   ├── [x] profile 3 頁面 body 外部邊距（.svg-bg-container overflow hidden）
│   ├── [x] .ftco-section-blank-small 響應式（991/768/480 三級漸進）
│   └── [x] FPS/Cash 付款頁按鈕（flex 容器 + CTA +12%/-12% + 行動 56%/44%）
├── [x] FPS 付款頁 logo 更換（fps-logo.png → fps_icon.svg，128px 桌面/110px 行動）— ✅ 2026-08-03
├── [x] 支付確認頁頂部圖標修復（fa-badge-check Pro-only → fa-circle-check Free）— ✅ 2026-08-03
├── [x] MCP 伺服器修復（django-manager 重建 + chrome-devtools 指向 Chromium）— ✅ 2026-08-03
├── [x] 訂單/付款頁面 UI 統一修復 — ✅ 2026-08-03（晚上）
│   ├── [x] Cash 頁面 fa-money-bill-wave 圖標 78px + .payment-icon-cash class
│   ├── [x] 訂單確認頁 4 個支付 Icon：.bc-payment-label-icon（1.3rem、水平對齊 + gap:12px）
│   ├── [x] 訂單確認頁背景文字改 data-bg-text + .bc-section-with-bg-text（::before 偽元素）
│   │   └── 獨立 div 置於導覽列/內容間、height:0 不佔文件流、top:150px、16vw、left:1vw
│   ├── [x] profile 3 頁面導航列統一 col-12 col-lg-8（與卡片同寬）
│   └── [x] 會員導航列響應式統一（移除 member_nav inline style，集中 bc-components.css：容器 flex 撐滿、按鈕 flex:1、行動 ≤576px 文字不縮放）
├── [x] owl_title_bg_02.svg 疊在影片動畫容器上方 + 向下微調 — ✅ 2026-08-08（commit 29b8527 已 push）
│   ├── [x] index.html 第二個 slider 新增 .banner-stage 定位層，SVG 移到 .inner（morph 容器）之外
│   ├── [x] style-utilities.css：.banner-stage 承接尺寸/置中角色，.inner absolute 填滿（morph+overflow 僅裁影片）
│   ├── [x] bc-components.css：錨點 .inner → .banner-stage；svg-box bottom 2rem→1.25rem 向下微調
│   └── [x] 驗證：SVG 四角完整不被 morph 裁剪 ✅
├── [x] home-slider 首屏跳動根因修復（bc-welcome-panel 被推下）— ✅ 2026-08-08
│   ├── [x] 根因：owl.carousel.min.css 的 .owl-carousel { display:none } → 初始化前橫幅高度 0
│   ├── [x] 修復：.owl-carousel.home-slider { display:block }（bc-components.css 後載入覆蓋）
│   ├── [x] 重要發現：Brewing 載入遮罩因 display:none 從未真正顯示過
│   └── [x] 驗證：無 JS vs 有 JS 面板位置 Y=695~819 完全一致 ✅
├── [x] 移除「Brewing 沖泡中」載入文字 — ✅ 2026-08-08
│   ├── [x] .home-slider::before content:"" 僅保留深黑遮罩；::after spinner 精確置中（-22px）
│   └── [x] 驗證：spinner 置中 X=737/Y=362、金色像素僅 40 個（無文字）✅
├── [x] 全域響應式審計修復（右側漏邊/橫向溢出/斷點統一）— ✅ 2026-08-09（分支 fix/responsive-breakpoints）
│   ├── [x] 診斷法：headless + 同源 iframe 掃描 scrollWidth vs innerWidth（診斷頁已刪除）
│   ├── [x] 根因1 首頁：.playfair_deco .lg_message flex 長文字不換行 → mid-banner 423px → 加 flex-wrap:wrap
│   ├── [x] 根因2 bean 詳情：.img-box .img-4 寬圖 455px → max-width:100% + object-fit:contain
│   ├── [x] 全域防護：html,body { overflow-x:hidden }（bc-components.css）
│   ├── [x] .cart-popup ≤575.98 移除 min-width:600px
│   ├── [x] 斷點統一：578→575.98、1196/1197→1200/1199.98；建立品牌斷點基準文件（480/575.98/767.98/991.98/1199.98/1200，880/1024/1250/1920 兼容保留）
│   ├── [x] base.html 版本參數 v=20260808b
│   └── [x] 驗證：所有頁面×所有斷點 scrollWidth<innerWidth 無橫向捲動 ✅
├── [x] 首頁第一個 slider 手機響應式調整（方案 A）— ✅ 2026-08-09
│   ├── [x] GIF 縮小：≤575 90%→55%、≤767 80%→65%（SVG .noise-img 不動）
│   ├── [x] 咖啡杯縮小：min(200px,60vw)→min(150px,45vw)；文字 max-width 100%→65% + 字體縮小
│   ├── [x] 根因：.slider-text height:750px 手機未縮小→照片偏下重疊 → 手機/平板高度=橫幅高（480/560px）
│   └── [x] 驗證：咖啡杯 y=279→144（上移居中）、重疊 191→148px、視覺協調（base.html v=20260808d）
├── [x] 導覽列 navbar-brand 手機響應式縮小 — ✅ 2026-08-09
│   ├── [x] ≤767.98 30px、≤575.98 22px（桌面 40px）
│   └── [x] 驗證：375/390/430px=22px、700px=30px、與 hamburger 無重疊（base.html v=20260808e）
├── [x] navbar logo 與菜單垂直居中強化 — ✅ 2026-08-09
│   ├── [x] .ftco_navbar .container align-items:center；brand/toggler/nav/nav-item align-self:center
│   ├── [x] .nav-link 改 flex + align-items:center（icon 與文字垂直對齊）
│   └── [x] 驗證：375px logo 46.5=toggler 46、1200px logo 70=菜單 70.5（base.html v=20260808f）
├── [x] navbar logo 略高修正（glyph 垂直補償）— ✅ 2026-08-09
│   ├── [x] 根因：Mogra glyph ascent 大 + line-height:1 → 視覺偏上 4px
│   ├── [x] 修正：navbar-brand padding-top +4px / padding-bottom -4px（box 高不變）
│   └── [x] 驗證：375px logo 47=navbar 47、768px 52≈54、1200px 73≈71（base.html v=20260808g）
├── [x] 隱藏 home-slider 橫幅內按鈕 — ✅ 2026-08-09
│   ├── [x] .owl-carousel.home-slider .slider-item .btn { display:none }（3 個 slider 按鈕全隱藏）
│   ├── [x] 順帶修復 navbar-brand 註解 /* 丟失的無效 CSS
│   └── [x] 驗證：7 個按鈕皆 display:none、visible=false（base.html v=20260808h）
├── [x] 隱藏首頁 Bean 咖啡豆區塊 + 分支合併推送 — ✅ 2026-08-09
│   ├── [x] index.html Bean 區塊 <section hidden>（使用者並行修改）
│   ├── [x] fix/responsive-breakpoints（7 commits）Fast-forward 合併回 main
│   └── [x] 推送 547abc5..263b052 main -> main（Render autoDeploy 已觸發）
├── [x] 全域響應式設計尺度系統（P1-P3）— ✅ 2026-08-09（分支 feat/responsive-system）
│   ├── [x] P1: responsive-system.css（--bc-fs-* / --bc-space-* 變數 + html 基準 + img max-width）
│   ├── [x] P2: heading-section / menu-wrap / btn 套用變數（375px h2 40→31px、subheading 58→35.5px）
│   ├── [x] P3: 全站驗證矩陣 9 頁面 × 3 斷點無溢出
│   └── [x] 教訓：覆蓋需注意 specificity（.coffee-menu-desc）
├── [x] heading-section 手機寬度修復 + 所有咖啡卡片網格 — ✅ 2026-08-09
│   ├── [x] ftco-subpage-banner 手機 padding 7em→1em（heading 375px 101→269px）
│   ├── [x] coffee_menu 新增 .bc-coffee-all-grid（名稱/描述/按鈕，3欄→2欄→1欄）
│   └── [x] 驗證：無溢出、btn 12.1/13.7/15.4px（base.html v=20260809g）
├── [x] 響應式系統分支 feat/responsive-system 完成並合併 — ✅ 2026-08-09
│   ├── [x] 9 commits：P1 系統 + P2 元件 + P3 驗證 + 各項修復（heading 寬度/卡片網格/按鈕/圖片/desc 字體）
│   ├── [x] 使用者並行：subpage-banner 手機高度 82vw、背景位置、bc-welcome 字體
│   └── [x] 合併推送 263b052..e28fb94 main -> main（Render autoDeploy 已觸發）
├── [x] bean_menu 使用 Z 字對角網格（與 coffee_menu 相同）— ✅ 2026-08-10
│   ├── [x] bean_menu.html extends 改 layouts/blank.html + 商品區換 .coffee-menu-zgrid（每 4 個一組 = 大 小 小 大）
│   ├── [x] 零新增 CSS：完全複用 .coffee-z-*（bc-components.css）響應式規則
│   └── [x] 驗證：cell 結構與 coffee_menu 逐標籤一致、7 斷點響應式狀態一致、無溢出
├── [x] title_bg_img_02.svg 行動端裁切修復 — ✅ 2026-08-10
│   ├── [x] 根因：.svg-bg-2 基礎 background-size:cover + 手機 width:300px → 直式 SVG 放大裁切
│   ├── [x] responsive-system.css ≤767px 手機改 auto+650px+top:-20px（與 .svg-bg 一致）+ base.html?v=20260809k
│   └── [x] 驗證：375/767px 兩頁一致無裁切；桌面維持 cover 不受影響
├── [x] 移除「所有咖啡/所有烘焙豆」section — ✅ 2026-08-10
│   ├── [x] coffee_menu/bean_menu.html 移除 .bc-coffee-all-grid section；responsive-system.css 清理 -46 行
│   └── [x] 驗證：兩頁無 bc-coffee 殘留、Z 網格保留、無溢出（base.html?v=20260809l）
├── [x] ani_bean_line_il 動畫全頁面橫跨修復 — ✅ 2026-08-10
│   ├── [x] 根因：absolute 無 left/top 落在 .post_archive（flex row-reverse）→ static position 偏移（只顯示 2/3）
│   ├── [x] 移到 .ftco-subpage-section 直接子層 + left:0 + width:100vw；selector 加 .ftco-subpage-section >
│   └── [x] 驗證：1280/768/390px 全寬、無溢出（animate-custom.css?v=20260810a）
├── [x] demido.jp SALON SEARCH aside 整合至首頁 — ✅ 2026-08-10
│   ├── [x] 新增 bc-search.css/js + search_img01/02/03.png（咖啡圖）+ bc_search_txt_pc/sp.svg；GSAP CDN
│   ├── [x] index.html 加 <aside class="bc-search">（連結→coffee_menu、文字品牌化、SALON_SEARCH SVG 保留原樣）
│   └── [x] 驗證：桌面 480px/手機 1160-900px、PC/SP 標題切換、hover is-on/is-out 正常、無溢出
├── [x] SALON SEARCH 滑鼠移出動畫修復 — ✅ 2026-08-10
│   ├── [x] 根因 1：.is-out 缺 transform（demido 有 -60%/-60%/-150%）
│   ├── [x] 根因 2：JS getComputedStyle transitionDuration 恆 0s → 立即移除 class 中斷動畫
│   ├── [x] 修復：補 is-out transform（桌面/手機 SP 位移）+ JS 改監聽 transitionend + setTimeout 保險 + done 旗標
│   └── [x] 驗證：CDP 追蹤 transform 序列完整播放（is-on 浮入→is-out 上移→1.3s 重置）（index.html?v=20260810b）
├── [x] cheesetart.com c-attract 流動選單整合 — ✅ 2026-08-10
│   ├── [x] 逆向分析：.c-attract 容器（負 margin 熱區 + perspective）+ .c-attract__layer（preserve-3d）+ UnifiedEffect（hover 3D tilt + elastic 回彈）
│   ├── [x] 新增 bc-attract.css/js + dots_40.svg；GSAP 全域化（base.html）
│   ├── [x] nav.html：漢堡選單三條線 c-attract + Buy & Order 按鈕（fixed 右側、垂直長條、白邊框、垂直文字）
│   ├── [x] hover「吸引」動畫：純位移跟隨游標（48px）+ proximity 60px 熱區；修復 maxTouchPoints 誤停用
│   ├── [x] #borders 右側全屏虛線（dots mask、z:9999、pointer-events:none、中心對齊按鈕）；左側隱藏
│   ├── [x] 按鈕：純黑背景、白色 1px 邊框、hover 邊框不變色、z-index 99999（最頂）
│   ├── [x] 浮動購物車：縮小 15%、黑底、白邊框、徽章縮小 10% 金底白字
│   ├── [x] 驗證：CDP 位置/顏色/z-index/動畫正確、無溢出
│   ├── [x] z-index 層級修復：`#ftco-navbar`（absolute + z:3）建立 stacking context → 按鈕 99999 被鎖在層級 3、低於虛線 9999；移到 `</nav>` 外 body 直接子層 → 99999 > 9999 按鈕最頂、虛線在下（nav.html）
│   ├── [x] 浮動購物車細部：徽章上移 3px（top -6px）、圖示縮小 10%（桌面 1.35rem/手機 1.17rem）
│   └── [x] 首頁第 3 個輪播橫幅：背景圖片 bg_3.jpg → 全幅背景影片（`.bc-banner-video` 複用，無橢圓遮罩/無 morph，roasting.webm 與第 2 個相同）；移除 .banner-video-3 橢圓死碼（index.html + style-utilities.css?v=20260810c）
├── [x] bean_menu A+C 混合不對稱網格（瀑布 × 棋盤翻轉）— ✅ 2026-08-10
│   ├── [x] 方案：瀑布交錯 A / 蛇形對角 B / L 形棋盤 C → 使用者選 A+C 混合
│   ├── [x] 每 3 個一組（大 小 小）：A 瀑布（大卡左側 1/3 + 小卡右側錯落 4rem）+ C 棋盤（組 2 翻轉 .bean-mix-flip 大卡右側 2/4）
│   ├── [x] 獨立 class .bean-mix-*（不影響 coffee_menu .coffee-menu-zgrid）；大卡 340px/小卡 260px
│   ├── [x] 教訓：小卡用 sibling 選擇器（.bean-mix-main + .bean-mix-sub），不用 nth-of-type（依賴 div 類型計數會錯位）
│   └── [x] 響應式 ≤1080 大卡整行+小卡並排、≤768 單欄；CDP 375/768/1280 驗證無溢出、coffee_menu 不受影響（bc-components.css?v=20260810i）
├── [x] 首頁第 1 個 slider coffee_border_01.svg 增大（×1.61）+ 改白色 — ✅ 2026-08-10
│   ├── [x] SVG：兩 path 加 fill="#ffffff"（原無 fill = 黑色，深色背景不可見）
│   ├── [x] CSS：.slider-item.slider-first .noise-img（index.html 加 slider-first class）各斷點 ×1.61（17vw→27.4vw/min 238→383px、55→88.6vw、65→104.7vw、75→120.8vw；手機維持 92vw cap = container 滿寬）
│   ├── [x] 教訓：不用 .slider-item:first-child（owl 結構每個 slider-item 都是其 owl-item 的 :first-child 會誤匹配全部），用明確 class
│   └── [x] CDP 1280/768/375/320 驗證 slider1 383/383/345/294px、slider3 不受影響、svg 白色、無溢出（style-utilities.css?v=20260810f）
├── [x] bean_menu banner 複製 footer .scroller-box 水平遮罩動畫 — ✅ 2026-08-10
│   ├── [x] banner container 內加 .scroller-box > .scroller > ul.tag-list.scroller__inner > 2×li；文案品牌化 ROASTED FRESH・BREWED FAST・SERVED FRESH・HUG IN A CUP
│   ├── [x] 零新增 CSS/JS：複用 .scroller-box 樣式；main.js querySelectorAll('.scroller') 全域初始化（data-animated=true + 複製 li 無縫循環）
│   ├── [x] 注意：banner 高度被內容自然撐高（桌面 500→701px、手機 82vw→482px），非 overflow 可接受
│   └── [x] CDP 驗證 data-animated=true、li 4 個、scroll 60s transform 位移中、footer 不受影響、無水平溢出
├── [x] bean_menu banner 跑馬燈換新：移除 .scroller-box → 自訂 .bc-marquee — ✅ 2026-08-10
│   ├── [x] 使用者選 Mogra 品牌字 20px、右→左、精簡 padding
│   ├── [x] bean_menu.html 換 .bc-marquee（2× item 無縫循環）；bc-components.css 新增 .bc-marquee（bcMarqueeScroll 28s、padding-block 0.75rem、prefers-reduced-motion 停用）
│   ├── [x] 純 CSS animation 不依賴 main.js（vs scroller-box 需 JS 複製 li）
│   └── [x] CDP 驗證 scroller-box 移除、動畫執行、banner 高度降（701→654px）、跑馬燈 60px、footer 不受影響（bc-components.css?v=20260810j）
├── [x] bc-marquee 無縫無限循環修正 — ✅ 2026-08-10
│   ├── [x] 問題：跑馬燈「斷開連接」→ 根因 .bc-marquee__item padding-inline 0.5rem 造成接縫 1rem 空白
│   ├── [x] 修正：移除 item padding、HTML 內容統一 &nbsp; 間距；無縫保證 = 2 item 等寬 + track 2×item + gap 0 + translateX(-50%)
│   └── [x] CDP 驗證 item 等寬、接縫 gap 0、動畫連續位移 -74→-128 無跳動（bc-components.css?v=20260810k）
├── [x] bc-marquee 抖動跳動修復 — ✅ 2026-08-10
│   ├── [x] 根因：letter-spacing 0.5px（亞像素字距累積）+ will-change transform（合成層子像素抖動）+ inline-flex sub-pixel 間隙
│   ├── [x] 修正：移除 letter-spacing、移除 will-change、track 改 display:flex + width:fit-content（與 footer scroller 一致）
│   └── [x] CDP 驗證 letter_spacing normal、will_change auto、4 次取樣位移完全均勻（每 800ms -16.4px）monotonic true 無跳動（bc-components.css?v=20260810l）
├── [ ] 咖啡/咖啡豆詳情頁按鈕間距（進行中，CSS 覆蓋/順序診斷）
├── [ ] 訂單搜尋/過濾功能
├── [ ] 批量操作（批量確認支付）
├── [ ] 訂單匯出功能
├── [x] 審計日誌系統（Audit Log）— ✅ 2026-07-28
│   ├── [x] AuditLog Model + Migration（action/order/staff/detail/IP/時間）
│   ├── [x] audit_logger.py（log_audit 工具函數，try/except 保護）
│   ├── [x] status_changer.py 整合審計日誌（7 個操作全部記錄）
│   ├── [x] API 端點 /eshop/api/audit-logs/（過濾/分頁/CSV 匯出）
│   ├── [x] 獨立前端頁面 templates/admin/audit_log.html
│   ├── [x] 訂單號碼統一（全部顯示簡短 order.id #2702）
│   ├── [x] WhatsApp 通知改為簡短訂單號
│   ├── [x] 訂單管理頁面「審計日誌」入口按鈕
│   └── [x] 端到端驗證通過（API Total: 12 筆）
├── [x] CSS 三步統一方案（2026-07-29）— ✅
├── [x] CSS 統一 Phase A: checkout.html 結帳頁（5 class）— ✅ 2026-07-30
├── [x] CSS 統一 Phase B: coffee + bean 商品頁（5 class）— ✅
├── [x] CSS 統一 Phase C: slideout_cart 購物車抽屜（3 class）— ✅
├── [x] CSS 統一 Phase D: base.html 全域模板（1 class）— ✅
├── [x] CSS 統一 Phase E: login+email_confirm+index（6 class）— ✅
├── [x] CSS 統一 Phase F: audit_log+websocket_monitor（4 class）— ✅
│   └── 🌐 全站 ~30 class，22+ HTML 模板，0 處靜態 inline 殘留
│   ├── [x] Step 1: 大圖 105x110 inline → bc-order-item-img-container（5 JS 檔）
│   ├── [x] Step 2: 小圖 80x80 inline → bc-order-item-img-sm（common-utils.js）
│   ├── [x] Step 3: HTML inline → btn-audit-log class（staff_order_management.html）
│   └── [x] 共 8 檔受影響，驗證腳本 verify_css_unify.js

Phase 3G：渲染器狀態轉換即時同步修復（優先級：P1）— ✅ 2026-07-27
├── [x] _executeOrderAction await loadUnifiedData + notifyAllListeners
├── [x] registerListener 直接渲染（跳過 isActiveTab 檢查）
├── [x] isLoading 鎖修復（_pendingRefreshAfterLoad flag）
├── [x] queue-manager.js startPreparation await + notifyAllListeners
├── [x] cleanupTimers 不再停止全域 timer（倒數計時器延遲修復）
├── [x] 子徽章空訂單歸零修復
├── [x] verify_flow_fix.py 端到端驗證腳本（50/50 測試通過）

Phase 4：效能優化與架構精簡 — ✅ 2026-07-27
├── [x] A1: 統一 all_data 渲染，消除 redundant 通知
├── [x] A2: 消除 QueueManager 冗餘監聽
├── [x] A3: BadgeManager 只監聽 all_data，統一數據入口
├── [x] B1: 增量 DOM 更新
│
├── [x] refactor: 建立 OrderActionService 共用服務（329 行新檔案）
│   ├── [x] 統一 7 個業務操作方法
│   ├── [x] QueueManager startPreparation 改用共用服務（-135 行）
│   └── [x] v2 Renderers _executeOrderAction 改用共用服務（保留降級方案）
│
├── [x] refactor: 淘汰 QueueManager 舊版 preparing/ready UI
│   ├── [x] QueueManager 只監聽 waiting_orders（維持等待卡片 UI）
│   ├── [x] preparing/ready/completed 全部由 v2 Renderers 管理
│   └── [x] 消除雙重渲染（-66%）
│
└── [x] feat: API 增量返回（scheme B）
    ├── [x] 支援 ?since=<ISO timestamp> 參數
    ├── [x] 首次請求返回全量，後續返回增量
    └── [x] 徽章摘要 always 返回最新
```

## UI 開發設計注意事項

進行所有 UI 開發設計和修改時，請遵循以下原則（詳見 `.clinerules/uiux-principles.md`）：

### 核心原則
- **不花巧** — 功能優先，裝飾其次
- **無需動畫** — 避免不必要的 CSS/JS 動畫（品牌已有的 noise_animation 除外）
- **無需漸變色** — 使用純色，不使用 `linear-gradient`、`radial-gradient` 等

### 風格統一性
所有 UI 元素必須保持以下五個面向的統一：
1. **元素元件** — 卡片、按鈕、輸入框、列表等元件風格一致
2. **字體** — 嚴格遵循字型系統規範（Noto Serif TC / Noto Sans TC / Mogra）
3. **按鈕** — 統一使用 `.bc-btn` 系列樣式，不自行定義新按鈕風格
4. **色彩** — 嚴格使用品牌色系統中的顏色值，不引入新色
5. **圖標** — 優先使用 icomoon 字型圖標，不引入新圖標集

### 品牌視覺設計風格
- **SVG 插圖**: 全域使用手繪趣味性風格 SVG 插圖
- **黑色主題背景**: 全域黑色 theme 背景
- **產品照片**: 使用無顏色背景的 PNG 去背圖
- **包裝設計**: 統一風格，每種口味不同配色
- **設計語言**: 清晰含空間感、簡潔留白、字體清晰
- **背景裝飾**: 大多數頁面背景有透明度低的 SVG 插畫元素，附有 CSS 動畫 `noise_animation`，豐富視覺層次
- **背景訊息字體**: 頁面背景有透明度低的品牌字體元素（`.title-bg-head`），與頁面內容相關

## 完整參考
詳細系統分析請參閱根目錄 `.clinerules/.clinerules`
UI 設計原則請參閱 `.clinerules/uiux-principles.md`