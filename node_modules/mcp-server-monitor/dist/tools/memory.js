import si from "systeminformation";
import { formatBytes, formatPercent } from "../utils.js";
export function registerMemoryTools(server) {
    server.tool("memory_info", "Get detailed memory usage: total, used, free, cached, swap.", {}, async () => {
        const mem = await si.mem();
        const usagePercent = (mem.used / mem.total) * 100;
        const swapPercent = mem.swaptotal > 0 ? (mem.swapused / mem.swaptotal) * 100 : 0;
        const bar = "█".repeat(Math.floor(usagePercent / 5)) + "░".repeat(20 - Math.floor(usagePercent / 5));
        const swapBar = mem.swaptotal > 0
            ? "█".repeat(Math.floor(swapPercent / 5)) + "░".repeat(20 - Math.floor(swapPercent / 5))
            : "N/A";
        const lines = [
            `💾  Memory Usage`,
            `═══════════════════════════════════`,
            `RAM:  [${bar}] ${formatPercent(usagePercent)}`,
            ``,
            `Total:      ${formatBytes(mem.total)}`,
            `Used:       ${formatBytes(mem.used)}`,
            `Free:       ${formatBytes(mem.free)}`,
            `Active:     ${formatBytes(mem.active)}`,
            `Available:  ${formatBytes(mem.available)}`,
            `Buffcache:  ${formatBytes(mem.buffcache)}`,
            ``,
            `Swap: [${swapBar}] ${mem.swaptotal > 0 ? formatPercent(swapPercent) : "N/A"}`,
            `Swap Total: ${formatBytes(mem.swaptotal)}`,
            `Swap Used:  ${formatBytes(mem.swapused)}`,
            `Swap Free:  ${formatBytes(mem.swapfree)}`,
        ];
        return { content: [{ type: "text", text: lines.join("\n") }] };
    });
    server.tool("memory_layout", "Get physical memory module details: type, size, speed, manufacturer.", {}, async () => {
        const layout = await si.memLayout();
        if (layout.length === 0) {
            return { content: [{ type: "text", text: "No memory layout data available." }] };
        }
        const lines = [`🧩  Memory Modules (${layout.length})`, `═══════════════════════════════════`];
        layout.forEach((m, i) => {
            lines.push(``, `Module ${i}:`, `  Size:          ${formatBytes(m.size)}`, `  Type:          ${m.type}`, `  Speed:         ${m.clockSpeed} MHz`, `  Form Factor:   ${m.formFactor}`, `  Manufacturer:  ${m.manufacturer}`, `  Part Number:   ${m.partNum}`, `  Voltage:       ${m.voltageConfigured}V`);
        });
        return { content: [{ type: "text", text: lines.join("\n") }] };
    });
}
//# sourceMappingURL=memory.js.map