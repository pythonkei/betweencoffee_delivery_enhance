import si from "systeminformation";
import { getSystemSummary } from "../utils.js";
export function registerSystemTools(server) {
    server.tool("system_overview", "Get a complete system overview: OS, CPU, memory, uptime. Use this as your first call to understand the server state.", {}, async () => {
        const summary = await getSystemSummary();
        const lines = [
            `🖥️  System Overview`,
            `═══════════════════════════════════`,
            `OS:        ${summary.os}`,
            `Hostname:  ${summary.hostname}`,
            `Platform:  ${summary.platform}`,
            `Kernel:    ${summary.kernel}`,
            `Uptime:    ${summary.uptime}`,
            ``,
            `CPU:       ${summary.cpu}`,
            `CPU Load:  ${summary.cpuLoad}`,
            ``,
            `Memory:    ${summary.memoryUsed} / ${summary.memoryTotal} (${summary.memoryUsage})`,
            `Free:      ${summary.memoryFree}`,
        ];
        return { content: [{ type: "text", text: lines.join("\n") }] };
    });
    server.tool("system_uptime", "Get system uptime and boot time.", {}, async () => {
        const time = await si.time();
        const bootDate = new Date(Date.now() - time.uptime * 1000);
        const lines = [
            `⏱️  Uptime`,
            `Current:    ${new Date().toISOString()}`,
            `Boot time:  ${bootDate.toISOString()}`,
            `Uptime:     ${Math.floor(time.uptime / 86400)}d ${Math.floor((time.uptime % 86400) / 3600)}h ${Math.floor((time.uptime % 3600) / 60)}m`,
        ];
        return { content: [{ type: "text", text: lines.join("\n") }] };
    });
    server.tool("system_users", "Get currently logged in users.", {}, async () => {
        const users = await si.users();
        if (users.length === 0) {
            return { content: [{ type: "text", text: "No users currently logged in." }] };
        }
        const lines = [`👥  Logged-in Users (${users.length})`, `─────────────────────────────`];
        for (const u of users) {
            lines.push(`  ${u.user} | ${u.tty} | ${u.date} ${u.time} | ${u.ip || "local"}`);
        }
        return { content: [{ type: "text", text: lines.join("\n") }] };
    });
}
//# sourceMappingURL=system.js.map