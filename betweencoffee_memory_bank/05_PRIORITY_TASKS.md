# 05_PRIORITY_TASKS

> **最後更新**: 2026-09-21

> ⚠️ **待部署**：本批改動（**浮動購物車改版（純前端）**／**咖啡豆自訂選項組**／氣泡 6 落點／退場動畫／Admin 精簡／留白與照片尺寸／GUNTE hero 平板尺寸修正）尚未上正式站 —— Render `autoDeploy=no`，需在 dashboard 按 **Manual Deploy**（部署時自動套用 migration **0070、0071、0072、0073**），或提供 `RENDER_API_KEY` 以 API 觸發。

## 近期優先
- **浮動購物車改版（✅ 完成 2026-09-21，純前端、無 migration）**：使用者三項指示 → ① **首頁隱藏 yama「購物車 [ n ]」列**（`.bc-yama .header-bot{display:none!important}`，markup/JS 保留可逆）；② **浮動購物車全站改到 `.bc-attract-buy` 正上方**、中心線對齊、直徑同按鈕寬；③ **只顯示購物袋圖示**（無圓形底／無邊框／無陰影）、圖示放大。定案尺寸：**桌機 37／平板 34／手機 31px**（2026-09-21 使用者追加「圖示縮小一點」；另設 44px 透明點擊區下限，手機只縮字形、可點範圍不變）；**同日追加 ②（使用者三項，2026-09-21）**：⑴ **空車也顯示圖示**（原本 0 件時整個隱藏 → 改為常駐顯示、**只隱藏數字圓圈**：`base.html` 的 wrapper 移除 `{% if not cart_count %}display:none` 且 badge 在 0 時 inline `display:none`；JS `_showFloatingCart()` 移除空車早退、`_updateFloatingCartVisibility()` 不再依數量隱藏、`_updateBadge()` 只切換浮動鈕 badge 的 display；抽屜開啟時仍暫時隱藏）；⑵ 圖示**再縮**（同日第三次指示：**−15% → 37／34／31px**）；⑶ 與 `.bc-attract-buy` **增加間距**（新變數 `--bc-fc-gap`，定義於 `.bc-floating-cart`；**0 → 12px → 20px**，各斷點退化 top 同步跟進，JS 定位與首頁 `place()` 共用同一來源）；圖示 5 選項 `shopping_bag`（目前）／`local_mall`／`shopping_basket`／`work`／`backpack`，預覽頁 `static/preview/bc-floating-cart-icons.html`（**選定後只改 `base.html` 一行**）。實作：`bc-slideout-cart.js` 新增 `_placeFloatingCart()`／`_bindFloatingCartPlacement()`（量可見長條 `.bc-attract-buy__link`、定位用實際佔位 `max(44, 字級)`、right 用 `clientWidth` 修正捲軸寬、量測後自我修正一次、動畫期間不量）；`bc-components.css` 改純圖示＋各斷點退化值；`bc-yama-header.css` 移除首頁「讓位到左側」兩處。**三個坑**：⚠ z-index 必須 ≥100000（否則被 Buy 按鈕的 `.c-attract` 熱區 `<a>` 蓋住、點擊誤跳選單）；⚠ **首頁正上方：`index.html` 的 `place()` 於 2026-09-21 追加讓位**（`buyTop = anchor + max(44, --bc-fc-size) + --attract + hB/2`，改讓「購物袋 top」對齊 navbar-brand 頂邊 → 首頁堆疊＝購物袋→Order→個人圓鈕；值皆讀 DOM/CSS，`_placeFloatingCart` 的「下方」fallback 只剩極端情況）；⚠ `_lockScroll` 的 right 補償改讀 `--bc-fc-right`。驗證 `docs/verify/cdp_verify_floating_cart.py`（5 斷點 × 首頁/咖啡詳情/豆詳情/購物車，含**圖示缺字偵測**）**全部通過**＋CDP 截圖目視；版號 bc-components.css／bc-slideout-cart.js／bc-yama-header.css **?v=20260921f**。⚠ 快取教訓：WhiteNoise `WHITENOISE_MAX_AGE=1 年`，**改檔必同步升 `?v=`**，否則瀏覽器沿用舊檔（本次驗證一度因此拿到舊 JS）。
- **咖啡豆自訂選項組（✅ 完成 2026-09-21）**：BeanItem 的「產地＋研磨」納入與咖啡同一套自訂選項組機制（Admin 勾選啟用＋排序數字、詳情頁依排序渲染、extra_options→購物車/訂單/員工端）。**使用者定案：產地不是讓客人選** → `BEAN_OPTION_GROUPS` 3 組：產地（唯讀顯示，金色晶片、不送值、不進購物車）／研磨（客人可選，沿用既有 `grinding_level` 欄位與舊訂單顯示）／烘焙度（唯讀、預設關閉）。資料：migration **0071**（BeanItem 6 欄位）＋**0072**（產地自由文字→代碼；「埃塞俄比亞 拼配 巴西」等→`OT`＋原文保留 `origin_custom`，零資料損失）。顯示端移除硬寫研磨對照（`translate_option` 改 `ALL_OPTION_GROUPS`、研磨併入 extra_options 時抑制 `grinding_level_cn` 避免重複）。驗證 `docs/verify/verify_bean_group_order.py` **48/48**（含咖啡端 16 組零回歸、新舊訂單相容）；headless 豆1/3/6 與咖啡4 渲染與排序實測；版號 bc-components.css ?v=20260921a。**若後續要新增豆的選項組**：只需在 `option_definitions.py` 的 `BEAN_OPTION_GROUPS` 加一組（`customer_selectable: True`、key 建議與 BeanItem 同名字段一致）＋ BeanItem 加 `option_<key>`/`option_order_<key>` 欄位＋migration，Admin 與前台（按鈕 UI）會自動出現。
- **產地改回「不新增 UI」（✅ 完成 2026-09-21，使用者回饋）**：使用者指示「咖啡豆產地…不需要新 UI 渲染」→ 產地恢復詳情頁**原有排版**（`block-23` 清單行），新增的唯讀晶片 UI 整套移除（`.bc-option-readonly*` 已刪、`bc-components.css` 淨變更 0、版號維持 ?v=20260915e）；**烘焙度退出選項組**（已有「烘焙水平」刻度）；Admin 唯讀組只留勾選（標示「唯讀 · 沿用原有排版」），勾選＝是否顯示產地那一行；migration **0073** 移除 `option_order_origin`/`option_roast_level`/`option_order_roast_level`。驗證 49/49（含取消勾選後產地行消失）。
1. **GUNTE hero 平板（iPad）尺寸過大修正**（✅ 完成 2026-09-16）：使用者回報 iPad Pro／Mini／Air 上 hero 過大 → 確認是**斷點問題**（專案標準斷點為純寬度，iPad 橫向 CSS 寬 1024~1366px 全部落 ≥992 桌機層，桌機版 hero 961px 對 768~834px 高的橫向 iPad 溢出 129~217px；實測 1024×768 超出 193px）。修法：① 新增短視窗規則 `@media (min-width: 992px) and (max-height: 900px)` → `--bc-gunte-scale: 0.55`／`--bc-gunte-top-gap: 90px`／`--bc-gunte-nav-gap: 140px`，實測 1024×768 與 1194×834 hero 皆 **694px**、1440×900 由超出 61px 變多 207px 餘裕；高度 >900px 桌機不動（1366×1024 仍 961px）。② 手機層邊界 ≤767.98 → **≤743.98**：iPad Mini 6 直向 744px 改走平板版（hero 819px、hide-sp 304.6px）；手機 375 完全不變。手套圓 `--circle-radius: 100vw` 未動（原設計）。6 尺寸 ×35 點 hit-test 全部 **0 遮擋**；版號 bc-gunte-hero.css ?v=20260916a。**同日追加（投影片文字等比化）**：使用者回報平板端 `hero_slide_item_cap`／`hero_slide_item_en` 文字未跟著縮、位置跑掉（參考桌面端）→ 量測確認 cap（固定 14px／top 20／right 16）在圖片縮到 297~344px 時占比由桌機 3.24% 漲到 4.07~4.71%、en 更在 ≤991.98 被 `display:none`（桌面是顯示）。修法：新增單一來源 `--bc-item-w`（投影片＝圖片寬）＋ `--bc-item-k: calc(var(--bc-item-w)/432)`，cap／en／ttl 全部改由它等比換算；平板改為顯示 en（手機仍隱藏）。實測 9 尺寸 cap 全 3.24%、en 全 2.78%，圖片寬／hero 高／nav 遮擋零回歸（0/35）；版號 ?v=20260916b。**同日追加（手機端改真 3:4 直式 sp 圖）**：使用者發現手機端 `.hero_slide_item_img` 是正方形（原站為長方形）→ 根因是本專案 `vis-sp` 與 `hide-sp` 指向同一張**正方形**檔，加上全域 `responsive-system.css` 的 `img { height: auto }` 讓高度依圖檔真實比例（標記的 `width=255 height=340` 輸給 intrinsic ratio）。修法：`scripts/make_hero_sp_20260916.py` 以 Pillow 置中裁切 3:4 → 產出 `static/images/01~07_sp_510x680.webp`（原站 `-sp@2x` 同規格 510×680、12~48KB），模板只換 7 個 `vis-sp` src、CSS 補 `img.vis-sp { width: var(--bc-item-w); height: auto }`。實測手機渲染 **255×340（0.750）**，平板/桌機尺寸與 hero 高零回歸（0/35 遮擋）；⚠ 手機 hero 636→721px，iPhone SE（667）需滾動 54px（未補償）；版號 bc-gunte-hero.css ?v=20260916c。**同日追加（cap／en 右移＋catch 標語居中與縮小）**：全斷點 cap 距圖右緣 30px（原站應 16px）→ 原因是全域 h3 reset 給直排標題外加 `margin: 0 14px 8px 14px` → cap 明確 `margin: 0` ＋ `right: calc(k*14)`（30→**14px**）、en margin `*10`→`*6`（10→**6px**）；桌面 catch（WE CAN MAKE...）水平本就居中、實測是**垂直差 64px**（catch 容器為 wrap 的 padding box、照片在 top-gap 135px 之後）→ `.hero_catch` padding 改 `var(--bc-gunte-top-gap) 5% 0`（平板/手機覆寫 0；平板若給 120px 反而低 63.5px）→ **全斷點垂直差統一 −3.5px**；字級 `clamp(30,4.6vw,68)` → **`clamp(24,3.4vw,50)`**（−26%）；hero 高與 nav 不變；版號 ?v=20260916d
1. **咖啡詳情頁 staff-comment 氣泡**（✅ 完成 2026-09-13，2026-09-14 微調＋DB 化＋幾何可設定）：三顆氣泡文字由 `CoffeeItem.staff_comment_1/2/3` 驅動（Admin 逐款改字、留空不顯示）＋ 第三顆（pattern-2 / `blowing-pattern03a.svg` / p-2 右側下半）＋ **定位幾何每款可設定**（`bubble_safe_left/right`、`bubble_photo_ratio`、`bubble_scale`；留空時存檔以 Pillow 自動量測去背 PNG，換圖後清空再存即重新量測）→ 直式、橫式（Black Blend 1200×840）與其他比例（Flat White 840×1200）皆可用；內距依字數 `--bc-cb-n` 自動置中＋ **氣泡位置 6 選 1**（✅ 2026-09-15：每顆氣泡可在 Admin 選左上／左中間／左下／右上／右中間／右下，欄位 `bubble_pos_1/2/3`（migration 0070）；6 個落點都自動貼在咖啡杯外圍、所有斷點自動縮放；動畫效果與原站相同；CDP 實測預設值與改動前逐項相同）＋ **Admin 精簡**（同日移除「氣泡定位」欄位組：幾何值仍自動量測，只是不再顯示）＋ 三列整體上移（上−2%／中−7%／下−10%）＋ **退場動畫修正**（同日：原會瞬間跳回入場起點往上 40px 才縮小 → 改 `.is-hiding` ＋ 收縮原點 center center，原地縮小消失）＋ **6 落點逐點微調**（`--cb-out` 水平錯開）＋ **Admin 精簡**（移除「氣泡定位」欄位組）；版號 bc-coffee-comment.css ?v=20260915p、bc-coffee-comment.js ?v=20260915a
2. **版面留白與咖啡照片尺寸**（✅ 完成 2026-09-15）：`.ftco-section-blank-small` 累計縮減（桌機 224→112.9px、平板 80.6→65.3px、行動 53.8→22.9px）——樣式綁 class（改標籤無效），改在 `bc-components.css` 覆蓋；平板端咖啡照片 ×0.64（`style-custom.css` 768–1079.98 區塊）；**最後由使用者手動定案**：留白基礎 `2em 0` / `10vh`、≤991 與 ≤768 `2vh`、≤480 `1vh`；`.bc-page-content` 首個 section 上緣 100 / 72 / 60 / 60px；＋ **GUNTE hero slide 等比縮小 10%**（✅ 同日：單一旋鈕 `--bc-gunte-scale`（定案 **0.8**） 驅動容器寬／圖片／圈圈字／hero 高／手套圓，手機不動；版號 bc-gunte-hero.css ?v=20260915a；同日加 `--bc-gunte-top-gap: 135px` 讓桌面端與導覽列拉開 −47.3→+20.2px；再加 `--bc-gunte-nav-gap` 修正 nav 與手套圓照片重疊（桌機 205／平板 135／手機 115px，7 slide 全驗證 0 重疊））；版號 bc-components.css ?v=20260915e、style-custom.css ?v=20260915b
2. **逐款咖啡填入氣泡文字**（待做，Admin 操作）：在 CoffeeItem 的「詳情頁氣泡文字（留空=不顯示）」為每款填入 1~3 句（建議 5~7 字）。目前只有 Butter King 有第 1、3 句（第 2 句為空），其餘 7 款皆待填（照片幾何已自動備好，填字即生效）
3. **正式庫資料落差回補 + 資料統一**（✅ 完成 2026-09-15）：訂單 643 筆、帳號 13 個、咖啡佇列 354 筆補入正式庫（單一 transaction + PK 去重 + sequence 修正；腳本 `scripts/backfill_orders_to_prod_20260915.py --apply`、log `scripts/backfill_orders_to_prod_20260915.log`）；`fb_kei` 因正式庫已有同 username/email 帳號，本地 id 58 的訂單改掛正式 id 62（不重複建帳號）；13 個腳本測試帳號一律降權停用（`is_superuser/is_staff/is_active=False`）避免密碼與權限上正式站；5 個 varchar 欄位以 Django 模型為權威對齊（本地庫才是偏離者）；unique 撞號自動改派（order_number 29 筆 → `BC-YYYYMMDD-9NNN`、pickup_code 3 筆 → 9000+）。**後續資料統一**（`scripts/unify_prod_data_20260915.py --apply`）：發現本地 id 62（`test_customer`）與正式 id 62（`fb_kei`）是不同人的 **id 碰撞** → 以 username 為橋樑重新對齊全部 2661 筆訂單歸屬（更新 36 筆：31 筆無主→fb_kei、5 筆→test_customer）、補建 `test_customer`（降權+停用）、覆蓋咖啡名稱 8/9/10（→ 抹茉 / 果香浅煎 / Butter King）。驗證：帳號 40/40、訂單 2661/2661、佇列 1036/1036、訂單歸屬依 username 完全一致、咖啡名稱 0 差異（本地 dump 備份 `/tmp/betweencoffee_now.backup`，1.98MB 仍保留）
4. **Black Blend / Flat White 氣泡定位**（✅ 完成 2026-09-14）：不再受限於 860×1100 寫死的留白；已量測填入 Black Blend（15.08/80.25、比例 1.429、倍率 0.70）與 Flat White（18.1/80.71、比例 0.7、倍率 1.0），CDP 12 組驗證氣泡皆在 `.img-box` 內、距照片內容 6~16px。※ 註：正式庫這兩款其實是 860×1100 的其他照片（`coffee_03_1.png` / `coffee_02_1.png`），量測結果自動為預設值
4. **sake-mv 資源集中**（✅ 完成 2026-09-14）：`static/images/sake-mv/*` 11 檔搬至 `static/images/*`、資料夾刪除、CSS/模板路徑同步（bc-sake-mv.css ?v=20260914a）
5. **首頁 yama tax-nav 收尾**（進行中）：welcome 面板已隱藏、圓球主題色 #774f18 / hover #1a1919、deco 白色、tagline 兩態文字完成；＋ **平板層修正（✅ 2026-09-16）**：使用者回報「只顯示在 iPad Mini、Air/Pro 沒顯示＋平板模組過大」→ 實測發現模組全尺寸都有顯示，真因是 **768/769 尺寸斷崖**（≤768 `min(30.769vw,236px)` → Mini 直 744 圓徑 229px；≥769 桌機 `min(12.083vw,174px)` → Air 直 820 僅 99px／字 9.1px）；新增 `@media (min-width:744px) and (max-width:1199.98px)` 平板層統一 **150px 圓 / 13px 主文字 / 11px tagline**（frame 139px、deco 67.7%、bottom 24/right 20、header-bot 同步重算），實測 744→149、768~1194 全 150px 且不裁切；手機（115/132px）與桌機（174px）不變；版號 bc-yama-header.css ?v=20260916a（原 ?v=20260904o）。＋ **iOS 底部工具列遮擋修正（✅ 2026-09-16 追加）**：使用者回報「平板 Mini 只露出約 1/5、Air/Pro 看不到；手機露出約 1/2」→ headless 診斷 `position:fixed` 正常、祖先無 transform/overflow（圓球 100% 在視窗內）→ 真因是**行動 Safari 底部工具列蓋在 layout viewport 上**（由數字反推：iPhone 遮 ~58px、iPad ~119px）；修法：`bc-yama-header.js` 以 `visualViewport` 量測遮擋高度寫入 `--bc-vis-gap`（桌機恆 0），`bc-yama-header.css` 的 tax-nav（三層）與 header-bot（兩處）bottom 加 `var(--bc-vis-gap, 0px) + env(safe-area-inset-bottom, 0px)`；實測正常零回歸、模擬遮擋 120px 精確上移 120px；＋ **平板 header-bot 被切 ＋ 手機層再縮 20%（✅ 2026-09-16 追加 2）**：① header-bot 在 iPad Mini 被截（Air/Pro 正常）→ 真因是平板層圓球 bottom 24px 讓 `.header-bot` 的 `calc(bottom − 1.2em − 11px)` 變 **−3px**（1133px 寬）→ 圓球 bottom 改 **44px**（header-bot 同步）→ 實測 820~1194 底距 16~21px、全部完整可見，桌機不變（90/55px）；② 新增 ≤743.98 手機層 ×0.8（圓徑 `min(24.615vw,189px)`＝375→92px、主文字 12.8px、tagline 10.4px，frame/deco 為百分比自動縮放）；③ 另確認使用者先前「只露出 1/5」是 **DevTools 裝置模式未開 "fit to window" 的檢視假象** ✓。＋ **header-bot 全面顯示（✅ 2026-09-16 追加 3）**：使用者回報「① header-bot 在 iPad Mini 截掉、Air/Pro 正常 ② 手機端 header-bot 沒有顯示」→ 真因是**整段 header-bot 樣式包在 `@media (min-width: 769px)` 內**（≤768 沒有 `position:fixed` 等基礎樣式）＋ ≤768 又 `display:none` 隱藏 → Mini 直向 744/768 與手機看不到；把 display 打開還會變靜態 block 掉出文件流（實測視窗下方 1194~1508px）。修法：① 移除該 media 外包 ② ≤768 的 display:none → display:block ③ 平板層補 display:block + font-size 13px ④ 手機層補 `.tax-nav{bottom:44px}` 讓位 + `.header-bot{display:block;font-size:10px;bottom:calc(44px+gap−1.2em−8px);right:calc(10px+min(24.615vw,189px)/2)}`；實測 11 個尺寸（先捲動觸發 .is-view）**全部 block/opacity 1/完整可見**（手機底距 24px、平板 17px、桌機 55px 不變）；版號 css ?v=20260916e。最後 → index/about/bean_menu 375–1280 響應式全面檢查 + CSS 版號 + git push
6. **行動端 `.bc-reviews-section` 縮放**（待做）：加入 ≤768 media 縮小
7. **bean_menu REAL 模組整合驗收**：模組已插入頁面、背景圖隱藏；需最後響應式與 push
8. **記憶庫瘦身收尾**：驗證 auto_load 正常、更新所有引用 `.clinerules/uiux-principles.md` 的路徑為 `docs/uiux-full-reference.md`
9. **安全（依 docs/security/git-history-cleanup.md）**：確認支付寶金鑰 8/1 已於後台輪換；git filter-repo 清除歷史金鑰（init commit 曾含 alipay pem）；repo 建議改 private
