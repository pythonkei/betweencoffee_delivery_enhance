# 系統狀態

> **最後更新**: 2026年9月21日（本次：**咖啡豆自訂選項組（BeanItem 選項組）**——BeanItem 的「產地＋研磨」納入與咖啡**同一套**選項組機制（Admin 勾選啟用＋排序數字、詳情頁依排序渲染、extra_options→購物車/訂單/員工端）；**使用者定案「產地不是讓客人選」**：產地＝唯讀顯示（金色文字晶片、不送值、不進購物車）、研磨＝客人可選（沿用既有 `grinding_level` 欄位，舊訂單零相容成本）、烘焙度＝唯讀且預設關閉；migration **0071**（schema）＋**0072**（產地自由文字→代碼；拼配→`OT`＋原文保留 `origin_custom`）；驗證 `docs/verify/verify_bean_group_order.py` **48/48**；上次：正式庫資料回補＋統一；咖啡氣泡 6 落點、退場動畫修正、Admin 精簡；版面留白與平板端咖啡照片縮減後由使用者手動定案；記憶庫瘦身：歷史日誌已封存，本檔僅現況）
> **注意**: 完整變更日誌 → `docs/archive/04_SYSTEM_STATE_history_202607-202609.md`；逐筆細目 → `git log`。


## 系統狀態摘要

| 維度 | 狀態 | 備註 |
|------|:----:|------|
| 資料庫（Supabase） | ✅ 正常 | 2026-09-15：專案曾消失導致部署失敗，使用者於後台還原後恢復（pooler 需約 3 分鐘註冊 tenant）；還原點（07-27）與本地（08-16）的**資料落差已回補完成**——訂單 2661 / 佇列 1036 與本地一致；順帶以 **Django 模型為權威**對齊 5 個 varchar 欄位（email 80 / order_number 20 / payment_method 10 / phone 12 / pickup_code 4；本地庫才是偏離模型的一方）；同日再以 **username 為橋樑**完成**資料統一**（舊訂單歸屬 36 筆、補建 test_customer、咖啡名稱 8/9/10 覆蓋為本地版）→ 帳號 40/40、訂單歸屬與名稱 0 差異 |
| 核心功能 | ✅ 完整 | 下單→支付→製作→取餐 |
| WebSocket | ✅ 穩定 | 三層架構，InMemoryChannelLayer |
| 支付 | ✅ 4種 | PayPal/Alipay/FPS/現金 |
| 社交登入 | ✅ 已修復 | Google/Facebook |
| 忠誠度 | ✅ 基礎 | 積分+優惠券 |
| 員工管理 | ✅ 完整 | 隊列+付款確認 |
| 前端架構 | ✅ Sprint 1-2 | 基礎服務層 + 核心重構 |
| 員工端渲染器 v2 | ✅ Phase 2 完成 | BaseOrderRendererV2 + 4 個 v2 渲染器 |
| 測試 | ⚠️ 不足 | 覆蓋率低 |
| 監控 | ⚠️ 基礎 | 無告警系統 |
| 文檔 | ✅ 已重構 | 資訊收攏至 `.clinerules/` 目錄 |
| 2026-09 前端整合 | ✅ 進行中 | yama 圓球 header（首頁）/ bean（咖啡豆）REAL 模組 / reviews 行動縮放 / **咖啡詳情頁 staff-comment 氣泡：文字 DB 化、幾何自動量測、6 落點可選（Admin）、原地縮小退場、版面留白與平板照片縮減（2026-09-15）** |
| 記憶庫 | ✅ 已瘦身 | 2026-09-06：04 拆至 <1K、`.clinerules` 拆 core（自動注入）/完整參考（docs/uiux-full-reference.md） |

## 資料庫現狀

| 面向 | 本地開發 | Render 生產 |
|------|:--------:|:-----------:|
| 資料庫 | 本地 PostgreSQL | Supabase PostgreSQL |
| 連線 | `127.0.0.1:5432` | `pooler.supabase.com:6543` |
| 認證 | `postgres/postgres` | Supabase 專案憑證 |
| Migration | 手動執行 | 部署時自動執行 |
| 管理 | psql / pgAdmin | Supabase Dashboard |

## 近期變更（最近 10 筆標題；細節見封存檔與 git log）

- 2026-09-18 **Buy & Order 按鈕（`.bc-attract-buy`）三項修正**：① **內部圖示更換**（提供 10 個 Material Icons 選項）；② **`Order` 文字 `font-weight: 300 → 500`**；③ **桌面 DevTools 切換裝置後模組位移、無法回原位**。③ 有**兩個獨立根因**（皆以 headless 重現）：**(a) 磁吸位移殘留**——`bc-attract.js` 只在 `mousemove` 依 proximity 重算位移；實測游標移入熱區（距中心 55px < `attractRange` 60）後按鈕被推開至 `matrix(1,0,0,1,-48,0)`，之後**不再動滑鼠 1.6 秒仍停在 −48px**（沒有任何事件會讓它回原位）→ 切換裝置後就一直偏移；修法＝新增 `AttractEffect.prototype.reset()`（`isHovering=false`、位移歸零、GSAP 0.3s 回彈）並在 `resize`／`orientationchange`／`visualViewport.resize`／`document mouseleave` 時呼叫 → 實測切換裝置後 tx 由 **−48 → 0** ✓。**(b) 對齊值量到切換瞬間的中間值**——首頁 `place()` 以 `#ftco-navbar .navbar-brand` 為基準寫入 inline top（該值為**中心座標**，搭配 CSS `translateY(-50%)`），原本只在 rAF 跑一次，切換裝置當下 navbar 仍在重排 → 量到中間值：實測 375 得 **26px**（重新載入為 28）、834 得 **37**（應 32）、1440 得 **53**（應 56）；修法＝`onResize` 追加 **350ms 安定後再 `place()`** 一次，並補 `orientationchange`／`visualViewport.resize`／`document.fonts.ready` 觸發，另「量不到基準時**清空 inline top**」交回 CSS（`top:50%`）避免殘留舊值 → 實測切換 375/834/1440 後 inline top **45／64／81 與重新載入值完全相同** ✓（回到原位）。※ 過程中曾誤判「`br.top + scrollTop`＝捲動相依」而一度改用純 `br.top`，實測會在捲動狀態被下方防裁切 `Math.max` 下限吸到視窗頂 → **已改回**（navbar 為文件流內 absolute，`br.top` 隨捲動等量遞減 → 相加為定值＝**文件座標**，與捲動無關）。① 圖示：以 base.html 已載入的 **Material Icons**（不新增圖示集），先用 `/tmp/bc_icon_probe.js` 驗證 26 個候選皆存在（字形寬 24px、不退回文字），取 10 個做成 **`static/preview/bc-attract-buy-icons.html`** 預覽頁——直接載入正式 `bc-attract.css` → 按鈕尺寸與線上實測一致（桌機 **48×150**、手機 **24×78** ✓；另補 `.c-attract { box-sizing: border-box }` 對齊線上全域 reset，否則會變 88×198 ✗），並隨斷點變化、每卡另有 40px 放大圖示；10 個候選清單同步寫入 `layouts/nav.html` 註解（換圖示只改一個字），**最終依使用者選定（2026-09-18）為 `coffee`（3 號：咖啡杯，有杯盤）**（期間曾試 4 號 `emoji_food_beverage`、2 號 `local_cafe`、7 號 `restaurant_menu`）——每次只改 `layouts/nav.html` 一行；實測三尺寸皆為真字形（桌機 17.6px、平板 15.2px、手機 11.7px，皆等於字級＝非退回文字 ✓），按鈕尺寸 48×150／34×116／24×78 無變化。版號 bc-attract.css／bc-attract.js **?v=20260918a**。
- 2026-09-16 **首頁 `.bc-search` 手機端高度縮減 1/3**：使用者回報「在手機端 bc-search 整個模組比例太長了，縮減 1/3」。定位：模組高度是**各斷點寫死的固定 px**——桌機 `480px`、**≤768px `1160px`**、**≤575.98px `900px`**（皆在 `.bc-search-link`）→ 手機端因此非常長。修法（×2/3）：① ≤768px `height: 1160px → 773px`；② ≤575.98px `height: 900px → 600px`；③ **背景浮動圖的垂直位置同步 ×2/3**（維持與原版面相同的相對比例）：`bg-img-01 top 420→280`、`bg-img-03 top 151→101`、`bg-img-02 top -30→-20`（≤575.98 只覆寫 left/right/width、top 繼承 ≤768 故一併生效）；④ 左右位置與圖片寬度不動（只縮高度）。**驗證**（headless）：模組高 375/430 → **600px**（原 900）、768 → **773px**（原 1160）、桌機 1440 → **480px 不變**；四種尺寸皆 **`scrollHeight` 無溢出**（flex `space-between` + 標題/說明/文字/圓形箭頭皆自然收合）；背景圖 top 實測 280／101（桌機仍 -30／118）。版號 bc-search.css **?v=20260916a**。**同日追加：新長文爆版修正（使用者把 `.bc-search-txt` 換成公平貿易長文後）**——長文 + 上一輪的縮高造成爆版：實測（修前）手機 375 模組 600px、**內容底 920px（溢出 +360px）**、手機 430 內容底 710px（+150px）；根因是手機端 `.bc-search-inner-bottom` 為 flex row，圓形箭頭佔 120px → `.bc-search-txt` 被擠成僅 **127px 寬**（375px 時）成細長條 579px 高，且行高沿用 body 的 1.8~2 倍。修法（≤575.98px）：底部改 **`flex-direction: column`**（文字取滿寬）、`.bc-search-txt` 1.1rem→**1rem** 且 `line-height: 1.6`、`.bc-search-icon` 120→**90px**（svg 26px）+ `align-self: flex-end` + `margin-top: 16px`、模組高度 600→**680px**（實測 375 需 674px；仍比原 900px 縮 24%）。驗證：375／430 → 680px（內容底 640 = 可用底 640 ✓）、768 → 773px（713 = 713 ✓）、1024／1440 → 480px 不變 ✓，**全部無溢出**。同時收錄使用者新增資產（**尚未被任何模板引用**）：`static/images/bean_location_01.jpg`（1440×2000）、`bean_location_02.jpg`（1440×1334）、`bean_quality.png`（1366×667）、`butter.svg`（300×76）。版號 bc-search.css **?v=20260916b**。**2026-09-18 追加：手機端「只縮字級」→ 模組還原 600px**——使用者對「維持 680px／或縮字級回 600px」二選一回覆「**縮小字級**」。量測法（`/tmp/bc_fit.js`、`/tmp/bc_fit2.js`）：把 `.bc-search-link` 設 `height:auto` 量**自然總高**（目標 ≤600 = 內距 80 + 內容 520），再對 msg×txt 全組合掃描求最大可讀字級。375 基準（1.5／2.2／0.85／1rem）自然總高 **660px（爆 60px）**；**關鍵發現**：標題 `.bc-search-ttl-message` 在 375 **≥1.7rem 會換行成 4 行**、**≤1.65rem 才維持設計的 3 行**（`<br>`×2 的意圖）→ 省 44px，長文即可維持 1rem 好讀字級；另註 **root font-size 為響應式**（375 時 html = **14.625px**），故用 0.88rem 只有 12.87px 過小。定案（≤575.98px）：`label 1.5→1.3rem`、`message 2.2→1.6rem`、`lead 0.85→0.75rem`、`txt 1.25（≤768 層）→1rem`（8 行／14.6px）、`icon margin-top 16→12px`（圖示 90px 不動）、**`height 680→600px`**（＝原 900px 縮 1/3，與 ≤768 同策略）。驗證：375 → 模組 600px、自然高 **574px**（餘 26px）、長文 14.6px／8 行 ✓；430 → 600px ✓；768（773px）／1024／1440（480px）**零回歸** ✓，五尺寸 `scrollHeight` 全無溢出；截圖 `/home/kei/bc-search-375-600px.png`。版號 bc-search.css **?v=20260918a**。
- 2026-09-16 **`.bc-yama` tax-nav 手機/平板：行距加大、字級略縮、文字顏色與桌機一致**：使用者回報「手機與平板的兩行距離還是太近 ＋ 稍微縮小文字 ＋ 修正文字顏色與桌面端一致」。**量測後找到兩類問題**：① **顏色**：第一行字的 `color` 在兩層寫死不同——**桌機層 `#ba833a`（金）、≤768 層 `#fff`（白）** → 而平板層／手機層都沒覆寫顏色 → ≤768 的白色一路生效到手機（實測手機 430 的 `color` = `rgb(255,255,255)` ✗、桌機/平板 = `rgb(186,131,58)` ✓）→ 修法：把 ≤768 層的 `color: #fff` 改成 **`#ba833a`**（與桌機一致）。② **行距**：原本用 `vw` 等比（各層 ×1.5），但在小螢幕的 vw 值太小 → 實測平板 834 只有 **6.1px**、平板 1024 只有 **7.5px**、手機 430 只有 **4px**（使用者「太近」）→ 修法：**平板層改固定 `11px`**（與桌機同值）、**手機層改固定 `8px`**。③ **字級略縮（約 −8%）**：平板層 `p` `min(1.4vw,11px)` → `min(1.28vw,10px)`、第一行 `min(1.75vw,13px)` → `min(1.6vw,12px)`；手機層 `p` `min(2.051vw,10.4px)` → `min(1.887vw,9.6px)`、第一行 `min(2.462vw,12.8px)` → `min(2.265vw,11.8px)`。**驗證**（headless，注入登入版 markup）：五個尺寸（1440／1024／834／430／375）第一行色**全部 `rgb(186,131,58)`** ✓；兩行間距 = 桌機 **11**／平板 1024 **11**／平板 834 **11**／手機 430 **8**／手機 375 **8** px；字級 = 16／12／12／9.74／8.49px。版號 bc-yama-header.css **?v=20260916h**。
- 2026-09-16 **`.bc-yama` tax-nav 兩行文字下移微調 ＋ 行距加大**：使用者要求「『按照之前口味落單?』與『當前積分可減免 -5』（登入後兩行）向下微調位置，並增加兩行間距」。① **下移**：`.tax-nav a` 是 `flex column + justify-content:center` → 直接加 margin/padding 只會位移「一半」（置中吸收另一半），故改用 `position: relative; top: 0.25em`（em 隨各層 `p` 字級縮放：桌機 **3.50px**、≤768 3.25px、平板 **2.75px**、手機 **1.92px**）做純位移。② **行距**：加大第一行 `margin-block` 的 bottom（各層 ×1.5，vw 比例一併維持）：桌機 `min(.486vw,7px)`→**`min(.764vw,11px)`**、≤768 `min(.769vw,6px)`→`min(1.154vw,9px)`、平板 `min(.486vw,6px)`→`min(.729vw,9px)`、手機 `min(.615vw,4.8px)`→`min(.9225vw,7.2px)`。實測（headless，注入登入版 markup）：1440 的 `p.top` = 3.50px、第一行 `margin-block-end` = **11px**、實際兩行間距 **11.0px**；834 → 2.75px / 6.08px / **6.1px**；375 → 1.92px / 3.46px / **3.5px**；文字中心相對圓心由 +5.5 → **+9px**（下移生效）。※ 此組選擇器（`p` / `p > span:first-child`）登入前後共用 → 登入前（登入補充咖啡因! / 為自己充充電!）也一併套用；若要只改登入後需另加 class。版號 bc-yama-header.css **?v=20260916g**。
- 2026-09-16 **`.bc-yama` 圓球內圓改色（深咖啡 `#774f18` → 近黑 `#131211`）**：使用者先後給了兩行「內圓改為白色」→「內圓改為 #131211」，**以最後一行的 `#131211` 為準**。定位：`.bc-yama .tax-nav` 的圓球由兩層構成——**外框**＝`icn_tax_frame.png`（322×324 PNG、透明底、近黑 `#1f1414` 的環線，由 CSS `a:before` 以 `50%/contain` 貼上，無法用 CSS 改色）、**內圓（圓盤填色）**＝`a` 的 `background: var(--bc-yama-circle)`。修法：`static/css/bc-yama-header.css` 的 `:root` 變數 `--bc-yama-circle` 由 `#774f18` 改為 **`#131211`**（一處改動、桌面與 ≤768 兩層同時生效）。驗證（headless 1440×950）：變數值 `.bc-yama`/`:root` 皆 `#131211`、圓球 computed `backgroundColor` = **rgb(19,18,17)**、圓球區域截圖像素分析 **#131211 佔 57.8%**（主色）、中心點 = `#131211`。版號 bc-yama-header.css **?v=20260916f**。※ 註：hover 掃層仍為 `--bc-yama-circle-hover: #1a1919`，與新內圓色僅差 6 階 → hover 效果幾乎看不出來，若要有感可再調亮（待確認）。
- 2026-09-16 **全站隱藏右側垂直虛線（`#borders` 裝飾線）**：使用者要求「所有模板隱藏右側垂直虛線」。現況：`#borders` 只在 `templates/betweencoffee_delivery/base.html`（**全站共用** → 所有前台模板都有），內含 `<span class="bc-borders__right">`（`dots_40.svg` 點狀 mask、2px 灰色垂直虛線、fixed 全高、pointer-events:none）；**左側虛線 2026-08-10 已 `display: none`**，管理頁 `templates/admin/staff_order_management.html` 也早已用 inline CSS 隱藏整個 `#borders`（「管理介面不需顯示」）→ 只剩前台右側一條。修法：於 `static/css/bc-attract.css` 的 `#borders .bc-borders__right` 加上 `display: none`（與左側同樣處理、一行即可還原；下方 `right` 的 3 個斷點值 65/57/36/32px 全部保留未刪）。驗證（headless，1440×950）：`/`、`/coffee_menu/`、`/coffee/4/`、`/bean_menu/`、`/about/` 五頁皆 200，`.bc-borders__right` 的 `display` 全部 `none`、實際寬度 **0**，`#borders` 容器本身仍 `block`（無破版）；版號 bc-attract.css **?v=20260916a**。※ 註：`#borders` 現在兩側都隱藏、實質已無作用，若確定不再使用可整包移除（markup + CSS 44~65 行與 2 個 media 覆寫），待確認。**同日追加 2（使用者確認「OK, 清理」）：`#borders` 已整包移除**——① `base.html` 移除 `<div id="borders">…</div>` markup（**保留 `.bc-page-content { position: relative }`**，它是通用 wrapper 樣式）；② `bc-attract.css` 移除 `#borders`／`.bc-borders__inner`／`__right`／`__left` 全部規則與其 3 個 media 覆寫、以及 header 與 `.bc-attract-buy` 段落提及 `#borders` 的註解（大括號 72 → **64 平衡**）；③ `nav.html`／`bc-weather.css`／管理頁的說明改為歷史註記（保留脈絡）；④ 管理頁 `staff_order_management.html` 的 inline selector 清單移除 `#borders`（仍保留 `.bc-attract-nav`／`.bc-floating-cart`）。驗證（headless，1440×950）：`/`、`/coffee_menu/`、`/coffee/4/`、`/bean_menu/`、`/about/` 五頁皆 200、`#borders`／`.bc-borders__*` 元素 **0**、**Buy 按鈕仍存在且可見（position: fixed）**、`.bc-page-content` 仍 `relative` → 無回歸；送出 HTML `id="borders"` 0 次、送出 CSS `bc-borders` 0 次。版號 bc-attract.css **?v=20260916b**。腳本 `scripts/remove_borders_20260916.py`（dry-run／--apply）。
- 2026-09-16 **首頁 `.feed--video` 上下邊緣漏光修復（蒙版 SVG 路徑被截斷 ＋ 邊緣合成縫隙）**：使用者回報「feed feed--video 上下邊緣漏光了，遮罩有問題?」。查證：① 兩個 clipPath（`#mask` / `#feedMaskVideo`）的 `d` 被合併成單一路徑時**最後一個字母被截斷**（長度 4638、結尾 `…74.2764 2090.18` 少一個座標且無 `Z`）→ 瀏覽器持續報 `Error: <path> attribute d: Unexpected end of attribute. Expected number`，clip 幾何不完整。**修法**：改用 wondercrecue.com 原站的 4 個完整 `<path>`（同座標系 objectBoundingBox 0..2430 × 0..570，皆以 `Z` 正常收尾；1023 字元，遠小於原本 4638）→ console 錯誤歸零。② 影片組移除了原本的 `margin-top: -6px` 上緣補償（該補償正是第一組用來遮 GPU 合成縫隙的），且下緣無餘裕 → 實測上下各約 6px 漏出亮影片。**修法**：`.feed--video::before/::after` 改為絕對定位、上下各 `1.2%` 實色帶（rgba(17,16,19,1)，z-index 2 高於蒙版層與影片層；字母上緣在 5.88/570＝1.03%，1.2% 不會裁到字母；絕對定位不影響區塊高度）。③ 另發現 `landing.css` 的 `<link>` **沒有版號**（快取會拿到舊檔）→ 補 `?v=20260916a`（index.html 與 landing_v3.html）。**驗證**（headless + 截圖像素分析）：正確對齊區塊後，1440（576px）上緣 y0~6＝16~17（深色）、下緣 y570~575＝17；834（406px）上緣 0~4＝16、下緣 401~405＝16；375（246px）上緣 0~1＝17、下緣 243~245＝16 → **上下邊緣完全被深色覆蓋**；區塊高度 576/406/246 不變、console SVG 錯誤 0。腳本 `scripts/fix_feed_mask_20260916.py`（dry-run/--apply）。※ 教訓：先前用 `clip: {y: max(0, box.y)}` 截圖時區塊頂端在視窗外，實際量到的是區塊中段＋下一區塊 → 量測前務必把目標完整移入視窗。**同日追加：`feed__triangle` 死碼移除 ＋ 影片 poster 移除（初始顯示修正）**——① 使用者要求移除 `.feed` 與 `.feed--video` 內的 `<div hidden class="feed__triangle">`（原站三角形裝飾的死碼，且 `<img>` 指向 **外部 wondercrecue.com**）→ 移除 3 處 markup（index×2、landing_v3×1）＋ `landing.css` 的 3 條 `.feed__triangle` 規則（含 2 個 media 覆寫）→ 送出的 HTML 中 `feed__triangle` 0 次、**外部連結請求 0** ✓。② 使用者回報 `.feed--video`「初始顯示異常：先顯示 index_feed_image.png 這張照片、隨後才播影片」→ 原因是 `<video poster="…index_feed_image.png">`（JS 首屏 `preload="none"`，未進視口前只顯示 poster）→ **移除 poster 屬性**，未播放時改顯示容器底色 `rgba(17,16,19,1)`（與蒙版同色，切換不可見）；`bc-feed-video.js` 註解同步更正（「維持 poster」→「維持容器底色」）；版號 bc-feed-video.js ?v=20260916a。驗證：停用 JS 載入頁面 → `video.getAttribute('poster')` = **null** ✓、容器 `backgroundColor` = **rgb(17, 16, 19)** ✓、區塊高度仍 576px ✓。腳本 `scripts/cleanup_feed_20260916.py`（dry-run／--apply）。**同日追加 2：`.feed__clock` 死碼一併移除**——使用者要求「一起清掉」。模板中已無 `.feed__clock` 元素（grep templates/ → 0），`landing.css` 內卻還留著 5 條規則（基礎 / `:hover` 空規則 / `span` / ≤768 與 ≤576 兩個 media 覆寫）→ 以標記切片移除（`/* 懸浮時鐘樣式` → `@media screen and (max-width: 1200px) {` 之前，共 **51 行 / 1241 字元 / 5 條規則**），移除後 `.feed__clock` 0 次、大括號 47/47 平衡、相鄰的 `.feed__mask_middle` 與各 media 規則完整。版號 landing.css **?v=20260916b**（index.html 與 landing_v3.html）。腳本 `scripts/cleanup_feed_clock_20260916.py`（dry-run／--apply）。
- 2026-09-16 **yama tax-nav（黃圓公告球）平板層修正**：使用者回報「① 只顯示在 iPad Mini、Air/Pro 沒有顯示 ② 平板端整個模組過大」。實測（headless）發現模組其實**全尺寸都 `display:block` / `opacity:1`**，真正的問題是 **768/769 的尺寸斷崖**：≤768 用 `min(30.769vw, 236px)`（iPad Mini 6 直向 744 → 圓徑 **229px**，過大），≥769 走桌機層 `min(12.083vw, 174px)`（Air 直 820 → **99px**、主文字僅 **9.1px**；Pro 11 直 834 → 101px／9.3px；Pro 12.9 直 1024 → 124px／11.4px）→ 平板（Air/Pro）幾乎看不見，只有 Mini 看得到。修法（`bc-yama-header.css` 檔尾新增）：`@media screen and (min-width: 744px) and (max-width: 1199.98px)` **平板層**統一 圓徑 `min(20vw, 150px)`、主文字 `min(1.75vw, 13px)`、tagline `min(1.4vw, 11px)`、frame 圖 `min(18.5vw, 139px)`、deco `67.7%`、位置 `bottom: 24px / right: 20px`，`.header-bot`（購物車 [ n ]）的圓心／圓底座標同步重算；≤430 手機（115／132px）與 ≥1200 桌機（174px）**維持原值**。實測：744→**149px**、768／820／834／1024／1133／1180／1194→**150px**（統一）、主文字 13px（9 字寬 96px ≤ 圓內緣 127px → 不裁切）；Pro 12.9 橫 1366（桌機層）165px、1440 → 174px 不變。單一旋鈕＝平板層的 150px 與 13px。版號 bc-yama-header.css ?v=20260916a（原 ?v=20260904o）。**同日追加：iOS／行動端底部工具列遮擋修正**——使用者回報「平板仍是：iPad Mini 只露出約 1/5、Air/Pro 看不到；手機只露出約 1/2（位置是不是太低）」。headless 診斷：`.tax-nav` 的 `position: fixed` 正常（捲動前後 rect 完全不變）、祖先鏈 `div.bc-yama < div.bc-page-content < body` **無** transform/filter/contain/overflow:hidden、圓球 100% 在視窗內 → 排除 CSS 定位問題；真因＝**行動 Safari 的底部工具列／分頁列／首頁指示列蓋在 layout viewport 之上**（`position: fixed` 是以 layout viewport 定位，故 `bottom:20~24px` 的元素實際落在工具列之下）。由使用者數字反推遮擋高度：iPhone 約 **58px**（115px 圓露出 1/2）、iPad 約 **119px**（149px 圓露出 1/5）、Air/Pro 幾乎全遮。修法：`bc-yama-header.js` 新增第 5 段——以 `visualViewport` 量測 `innerHeight − vv.height − vv.offsetTop` 寫入 CSS 變數 `--bc-vis-gap`（上限 200px 防螢幕鍵盤、桌機恆為 0），並監聽 resize／scroll／orientationchange；`bc-yama-header.css` 三層的 `.tax-nav` bottom 與兩處 `.header-bot` bottom 全改為 `calc(<原值> + var(--bc-vis-gap, 0px) + env(safe-area-inset-bottom, 0px))`。實測（headless）：正常情況 gap=0、位置與改前逐項相同（手機 20／平板 24／桌機 90px → **零回歸**）；模擬遮擋 120px 時圓球精確上移 120px（375／744／820／1440 四尺寸皆然）。版號 bc-yama-header.css ?v=20260916b、bc-yama-header.js ?v=20260916a。**同日追加 2：DevTools fit-to-window 假象釐清 ＋ 平板 header-bot 被切 ＋ 手機層再縮 20%**——使用者回報「① header-bot _pc 在 iPad Mini 被截掉（Air/Pro 正常）② 手機端整個 .bc-yama 再縮小 20%」。① 真因：**平板層圓球 bottom 設 24px，下方 `.header-bot`（購物車 [ n ]）座標 `bottom: calc(<圓球bottom> − 1.2em − 11px)` 因而算出負值**（1133px 寬時 24 − 1.2em(16) − 11 = **−3px** 被切；820px 時僅 +1.5px 才看似正常 ＝ 越大台的平板切越多）→ 修法：平板層圓球 bottom 24 → **44px**（`.header-bot` 同步 44px）→ 實測 820/834/1024/1133/1180/1194 的 header-bot 底距 16~21px、**全部完整可見**；桌機 1440 不受影響（90／55px 不變）。② 新增 `@media (max-width: 743.98px)` 手機層，以 ≤768 原值 ×0.8：圓徑 `min(30.769vw,236px)` → **`min(24.615vw,189px)`**（375 → **92px**、430 → 106px）、主文字 `min(3.077vw,16px)` → `min(2.462vw,12.8px)`、tagline → `min(2.051vw,10.4px)`、主文字間距 ×0.8；frame 圖（92%）與 deco（67.7%）為百分比自動縮放；位置與 `--bc-vis-gap` 沿用 ≤768 不變；平板／桌機不受影響。另：使用者先前回報的「只露出 1/5」經確認是 **DevTools 裝置模式未開 "fit to window" 的檢視假象**（改開後顯示正常），非程式問題。**同日追加 3：header-bot 全面顯示（原本 ≤768 被隱藏）**——使用者回報「① header-bot 在 iPad Mini 截掉、Air/Pro 正常 ② 手機端 header-bot 沒有顯示」；追查發現**整段 header-bot 樣式被包在 `@media (min-width: 769px)` 內**（僅 ≥769 有 `position: fixed` 等基礎樣式），而 ≤768 又以 `display: none` 隱藏 → iPad Mini 直向 744/768、手機完全看不到；且一旦把 `display` 打開，因無 fixed 定位會變成靜態 block 掉到文件流外（實測落在視窗下方 1194~1508px）。修法：① 移除該 `@media (min-width: 769px)` 外包（header-bot 基礎樣式全寬度生效）② ≤768 的 `display: none` 改 `display: block` ③ 平板層 `.header-bot` 補 `display: block` + `font-size: 13px`（桌面 vw 公式在平板只剩 9~10px 過小）④ 手機層（≤743.98）補 `.tax-nav { bottom: 44px }` 讓出空間 + `.header-bot { display:block; font-size:10px; bottom: calc(44px + gap − 1.2em − 8px); right: calc(10px + min(24.615vw,189px)/2) }`。實測（先捲動觸發 `.is-view`）：**375／430／744／768／820／834／1024／1133／1180／1194／1440 全部 `display:block`、opacity 1、完整可見**（手機底距 24px、平板 17px、桌機 55px 不變）。版號 bc-yama-header.css ?v=20260916e。
- 2026-09-16 **GUNTE hero 平板（iPad）尺寸過大修正（斷點問題）**：使用者回報 iPad Pro／Mini／Air 上 hero 過大。根因：專案標準斷點是**純寬度**（≤767.98 手機／≤991.98 平板／≥992 桌機），而 iPad 橫向的 CSS 寬度是 1024~1366px → **全部落進桌機層**；桌機版 hero 總高 961px（wrap 648 ＋ nav 205 ＋ padding 90）是給 ≥900px 高視窗設計的，塞進 744~834px 高的橫向 iPad 就溢出。實測（修前）：iPad Mini 5/9.7 橫向 1024×768 → hero **961px vs 視窗 768＝超出 193px**、投影片圖片仍是固定 432px（占 slide 85%）；Air/Pro 11 橫向 1194×834 同樣；iPad Mini 6 直向 744×1133 則因 744 ≤ 767.98 而落**手機層**（只顯示 vis-sp 255px，在 744 寬螢幕偏小）。修法：① 新增 `@media (min-width: 992px) and (max-height: 900px)` 短視窗規則，三個旋鈕一起降一級（`--bc-gunte-scale: 0.55`、`--bc-gunte-top-gap: 90px`、`--bc-gunte-nav-gap: 140px`）→ 實測 1024×768 與 1194×834 hero **皆 694px**（多 75／141px 餘裕）、1440×900 由超出 61px → **多 207px 餘裕**；高度 >900px 的桌機完全不動（1366×1024 實測仍 961px／432px 圖／nav-gap 205）。② 手機層邊界 ≤767.98 → **≤743.98**：iPad Mini 6 直向 744px 改走平板版（hide-sp inline **304.6px**、splide 404.7px、hero **819px＝視窗高 72%**，與其他平板直向一致）。手套圓 `--circle-radius: 100vw` 未動（原設計；先前「縮小整圈咖啡圖弧」改動已被 revert）。驗證：7 個 nav 圓點各 5 點共 **35 點 hit-test**，744／1024／1194／1366／375／1440 六種尺寸**全部 0 遮擋**（※ 手套圓是 3D 旋轉環，`getBoundingClientRect` 量照片最低點會被旋轉 bbox 高估約 28px，須用 hit-test 判定）。版號 bc-gunte-hero.css ?v=20260916a；**同日追加：hero 投影片文字等比化（使用者回報平板端文字未跟著縮、位置跑掉）**——`.hero_slide_item_cap`（直排 "BLACK BLEND"，原固定 14px／top 20px／right 16px）、`.hero_slide_item_en`（原固定 12px／margin 10px）、`.hero_slide_item_ttl`（原 `540px×scale`）在圖片被平板 margin／短視窗 scale 縮到 297~344px 時仍維持原 px → 字級占圖寬由桌機的 3.24% 漲到 4.07~4.71%（en 2.78%→3.49~4.04%），且 `.hero_slide_item_en` 在 ≤991.98 被 `display:none`（桌面是顯示）→ 與桌面不一致。修法：新增**單一來源變數** `--bc-item-w`（＝投影片＝圖片寬度；桌機 `min(540px×scale, min(62vw,892px)×scale−24px)`、≤991.98 `(68vw×scale−24px)×0.8`、≤743.98 `255px`）＋ `--bc-item-k: calc(var(--bc-item-w) / 432)`（原設計 432px 圖的 1/432 比例），cap／en／ttl 的尺寸與位移全部改由它換算（`calc(var(--bc-item-k) * 14)` 等）；≤991.98 移除 `en { display:none }` 改與桌面一致顯示（≤743.98 仍隱藏，item margin 僅 10px 放不下）；手機維持原手調值（cap 10／15／12px、ttl 原 scale 尺寸 + `max-width: calc(100% + 11px)`）。實測（headless chromium，9 尺寸）：**cap 占圖寬全部 3.24%、en 全部 2.78%（＝桌機參考值）**，圖片寬／hero 高／nav 遮擋與改動前逐項相同（0/35）、`ttl` 寬占圖 1.000（手機 1.043 不變）。；**同日追加：手機端投影片改為真 3:4 直式 sp 圖（與原站一致）**——使用者發現手機端 `.hero_slide_item_img` 是正方形（原站是長方形）。根因：原站手機端另有獨立直式檔（`hero-slideNN-photo-sp@2x.jpg` = 510×680，3:4；桌機 `@2x` 才是 1080×1080），且原站 CSS 對 `.hero_slide_item_img` 只有 `overflow: hidden`（形狀全靠圖檔）；本專案 7 張 slide 的 `vis-sp` 與 `hide-sp` **指向同一張正方形檔**（01~03/07 = 1080×1080、04 = 660×660、05 = 630×630、06 = 720×720），加上 `base.html` 全域 `responsive-system.css` 的 `img { max-width:100%; height:auto }` → 高度依圖檔真實比例（1:1）→ 手機端渲染 255×255；標記上的 `width="255" height="340"`（瀏覽器讀成 `aspect-ratio: auto 255/340`）**輸給 replaced element 的 intrinsic ratio**。修法（A1）：新增 `scripts/make_hero_sp_20260916.py`（Pillow 置中裁切 3:4 → 統一輸出 510×680 WebP；支援 `--apply`、`--offset 逐張水平偏移 1~7`；並輸出對照拼圖 `/tmp/hero_sp_preview.png` 供目視），產出 `static/images/01~07_sp_510x680.webp`（12~48KB，原 1080 方圖 100KB+ → 手機省流量）；模板 7 個 `vis-sp` src 換新檔（`hide-sp` 不動、桌機維持正方形設計）；CSS 補 `.hero_slide_item_img > img.vis-sp { width: var(--bc-item-w); height: auto }`（沿用單一來源旋鈕，比例由圖檔本身決定、不需 object-fit）。裁切品質客觀檢查：7 張皆滿版照片（無單色背景），以邊緣能量分佈驗證「能量重心全落在裁切框內、框內保留 70~88% 視覺量」。實測（headless）：375/390/430 `vis-sp` natural **510×680**、渲染 **255×340（比例 0.750）**；平板/桌機 `vis-sp` display:none、`hide-sp` 尺寸（315／343.8／297／432）與 hero 高（829／858／694／961）與改動前逐項相同、nav 35 點 **0 遮擋** → **零回歸**；⚠ 已知代價：手機 hero 636 → **721px**（375×812 仍完整顯示 ✓），**iPhone SE 375×667 需滾動 54px**（未補償 padding，待決定）。；**同日追加：cap／en 右移與 catch 標語居中＋縮小**——① 使用者回報全斷點 `hero_slide_item_cap`／`hero_slide_item_en` 偏左：實測 cap 距圖片右緣 **30px**（原始檔 CSS 是 `right: 16px`，原站即 16px），原因是**全域 h3 reset 給這個直排標題外加了 margin `0px 14px 8px 14px`**（`style-bootstrap.css` 的 heading margin，rem 計算；手機因 html font-size 不同為 10px）→ 修法：cap 明確 `margin: 0` 並把 `right` 收為 `calc(var(--bc-item-k) * 14)`（30 → **14px**）；en 的 margin 由 `* 10` 收為 `* 6`（圖右緣到 en 左緣 10 → **6px**）。② 桌面端 catch（WE CAN MAKE THE RULES...）未與照片垂直居中＋字太大：實測**水平中心差 0px**（水平本來就居中）但**垂直差 64px**（catch 容器＝`.hero_wrap` 的 padding box、中心 324px；照片則因 `--bc-gunte-top-gap: 135px` 而下移到中心 388px）→ 修法：`.hero_catch` 的 padding 由 `0 5%` 改為 `var(--bc-gunte-top-gap) 5% 0`（桌機／短視窗）；**平板與手機必須覆寫為 0**（平板 `.hero_wrap` 是 `height:auto`，catch `height:100%` 解析為 wrap 的 padding box，且內容含 nav 高於內容框 → 加 120px 反而讓文字低 63.5px；pt:0 才是 −3.5px）→ **全斷點垂直差統一 −3.5px**（＝照片相對 splide 的固有偏移）；字級 `clamp(30px, 4.6vw, 68px)` → **`clamp(24px, 3.4vw, 50px)`**（−26%；1440 66.24→48.96、1920 68→50、1024×768 47.1→34.8、834 38.36→28.36、375 30→24）。hero 高（961／693.5／857.8／721.2）與 nav 遮擋不變。版號 bc-gunte-hero.css ?v=20260916d。
- 2026-09-15 咖啡氣泡位置 6 選 1：每顆氣泡可在 Admin 各自選落點（左上／左中間／左下／右上／右中間／右下），新增 `bubble_pos_1/2/3`（migration 0070）＋ Admin 欄位組「氣泡位置（6 選 1；動畫不變）」；CSS 用 `data-pos` 選配落點（垂直 3 列、水平左右 2 側，K 依該顆圖形寬度自動縮放）→ 6 個落點都在咖啡杯外圍；彈出動畫與原站完全相同；CDP 實測預設值與改動前逐項相同（left −52.2/337.8/333.0px）、改 `bl` 確實移到左下；版號 bc-coffee-comment.css ?v=20260915d；同日 Admin 精簡：移除「氣泡定位」欄位組（幾何值仍由存檔自動量測：換圖時 save() 會偵測 image 變更並自動重新量測，不再需要手動清空）；同日三列整體向上調整（上 −2%／中 −7%／下 −10%：桌機 37.58%−0.42245fs / 57% / 70%，手機 65.42%−0.42245fs / 71% / 74%）；同日修正氣泡「退場動畫」：原本消失前會瞬間跳回入場起點（往上 40px）才縮小 → 改為加 `.is-hiding`、收縮原點改 center center，氣泡在原位置縮小消失（實測三顆中心位移 dx/dy 皆 0）；同日把氣泡與杯子的左右留白由 0.5fs 加大為 1.5fs（`--cb-gap`，桌機 +17px／手機 +13px，可單值調整）；6 個落點改為各自高度、左右刻意錯開（桌機 tl36/ml52/bl67.5、tr40/mr61/br72.5；手機 62.5/67/72、66.5/74/76）以製造自然落差；同日逐點微調：垂直 +2%（tl38/ml54/bl67.5、tr42/mr61/br74.5）＋ 新增 `--cb-out` 逐點往杯外推（tl3%/ml2%/tr2%/br2%）；落差再加大：右上 42→36%、右中間 61→64%（錨點差 28% 照片高、氣泡體重疊 19%→9%）、左上 --cb-out 3→5%（距杯 47.1px）；再逐點微調（tl38/ml55/bl70.5、tr34/mr64/br76.5；--cb-out tl4/ml2/tr1/br2）；右側三點水平錯開（--cb-out tr1/mr5/br2% → X 中心差 14.3/10.7px）；右上再左移 2%（--cb-out tr -1%，負值＝往杯內收，tr↔mr 水平差 21.5px）；右上再左移 2%＋上移 2%（top 32%、--cb-out -3%，tr↔mr 水平差 28.7px）；上列再上移（tr 30%、tl 34%；凸出照片上緣 tl 9.4~35.3px、tr 27.7~53.6px）
- 2026-09-15 `.ftco-section-blank-small` 留白縮小 30%：樣式綁 class（非標籤），故在 bc-components.css 覆蓋 8em/26vh → 5.6em/18.2vh 並同步三組 media query（×0.7）；同日再縮 20%（全面 ×0.8）→ 實測 224px → 156.8px → 125.4px（累計 −44%）；版號 ?v=20260915b；同日再分斷點微調：行動端 −20%、平板端 −10%，另平板端咖啡照片 −20%（style-custom.css 768–1079.98 區塊 max-height 532.95→426.36px、max-width 88.825→71.06%）；版號 bc-components.css ?v=20260915c、style-custom.css ?v=20260915a；同日再一輪：平板端留白再 −10%（72.6→65.3px）、行動端再 −20%（28.7→22.9px）、平板端咖啡照片再 −20%（426.3→341.1px，累計 ×0.64）；版號 bc-components.css ?v=20260915d、style-custom.css ?v=20260915b；再追加桌面端 −10%（125.4→112.9px，累計為原值 0.504 倍）；版號 bc-components.css ?v=20260915e；**使用者手動定案（同日）**：`.ftco-section-blank-small` 基礎 `padding: 2em 0; min-height: 10vh`、≤991 與 ≤768 `min-height: 2vh`、≤480 `1vh`；`.bc-page-content` 首個 section 上緣 margin `100 / 72 / 60 / 60px`（桌機／768–991.98／576–767.98／≤575.98）；另 **GUNTE hero slide 等比縮小 10%**（`bc-gunte-hero.css` 新增單一旋鈕 `--bc-gunte-scale`（試 0.9 後定案 **0.8**），驅動容器寬／`img.hide-sp`／圈圈字 SVG／hero 高／手套圓；手機 ≤767.98 全部維持不變；版號 ?v=20260915a；同日加 `--bc-gunte-top-gap: 135px` 修正桌面端投影片頂端被導覽列蓋住（−47.3→+20.2px 淨空、hero 總高不變）；同日再加 `--bc-gunte-nav-gap`（桌機 205px：取手套圓旋轉最壞情況、淨空 23.3px；平板 60→135px、手機 20→115px）修正 hero_nav 與手套圓咖啡照片重疊，7 個 slide 全驗證 0 重疊）
（該 class 亦用於 index/bean/bean_menu/coffee_menu/checkout/landing_v3）
- 2026-09-15 部署事故排除：Supabase 專案消失（`tenant/user ... not found`、DNS NXDOMAIN）→ 使用者還原專案後，**pooler 約 3 分鐘才註冊 tenant**（連線字串不需改），觸發 Render 部署成功（0066~0069 套用、status live）
- 2026-09-14 sake-mv 資源集中（`static/images/sake-mv/*` → `static/images/*`，資料夾刪除、CSS/模板路徑同步）＋ 咖啡詳情頁氣泡微調（p-1 左 5%、p-3 上右 5%，5%＝氣泡自身尺寸）＋ 氣泡內文字垂直置中修正（對齊氣泡面積質心；pattern-1 上 1.1803fs/下 1.6497fs、pattern-5 上 0.9318fs/下 1.3972fs；4 斷點實測 ≤0.44px）＋ hero（GUNTE）第 6/7 張 slide 圖片更換（06_720.jpg / 07_1080.jpg）＋ **咖啡詳情頁氣泡文字 DB 化**（CoffeeItem `staff_comment_1/2/3` ＋ migration 0066/0067 ＋ Admin 欄位組；三顆氣泡對所有 coffee 生效、留空不顯示；新增 pattern-2（03a.svg）/p-2 位置；內距改依字數 `--bc-cb-n` 自動置中；4 斷點實測 ≤0.43px）＋ **氣泡定位幾何每款可設定**（`bubble_safe_left/right`、`bubble_photo_ratio`、`bubble_scale` ＋ migration 0068/0069；存檔時留空即以 Pillow 量測去背 PNG → Black Blend 15.08/80.25、比例 1.429、倍率 0.70；Flat White 18.1/80.71、比例 0.7；12 組 CDP 驗證氣泡皆在盒內、距內容 6~16px）
- 2026-09-13 咖啡詳情頁 staff-comment 氣泡移到咖啡杯外（對齊去背 PNG alpha bbox x 9.30%~89.53%，單一 calc 算式通吃 10 斷點、零重疊零溢出）
- 2026-09-11 咖啡詳情頁 staff-comment 氣泡建立（/coffee/10/）：長方形氣泡 + 咖啡專用 SVG + 直立文字 + 3 秒輪播彈出動畫
- 2026-09-01 ramen→bean REAL + reviews 整合（about 頁）＋ 平板照片放大置中 ＋ sake-shirakiku product-mv 整合
- 2026-08-27 Timber Wharf 全域應用 + weekday 徽章 + SERIES 資料庫驅動（咖啡/咖啡豆）
- 2026-08-26~27 yokohama-timberwharf weather 天氣/時鐘元件整合（about 頁右上角）
- 2026-08-25 sukima row-animation + content-block-title 氣泡 + capabilities-gallery 整合（about）
- 2026-08-25 sukima who section（.section.who）整合（about）
- 2026-08-24 sukima hero-title-svg 手繪圓形畫線動畫整合（about）
- 2026-08-24 Sticker 貼紙模組化（sticker-box 資料驅動）
- 2026-08-24 浮動購物車按鈕修復
- 2026-08-22~23 MCP 配置修復 + rotate-mau 整合 + 多項 UI 整合
- 2026-08-21 SERIES 照片修復 + 平板基於桌面縮小 + caption 加大
- 2026-09-21 **咖啡豆自訂選項組（BeanItem 選項組）**：使用者需求「咖啡豆也要像咖啡一樣有自訂選項組（產地＋研磨），並且可以自訂顯示排序」＋追問「產地不是讓客人選」→ 定案 **`customer_selectable` 旗標**：`BEAN_OPTION_GROUPS` 3 組——`origin`（產地，唯讀顯示）、`grinding_level`（研磨，客人可選）、`roast_level`（烘焙度，唯讀、預設關閉）。**關鍵設計**：① 各組的 key **與 BeanItem 既有同名欄位一致**（值/預設值來源就是該欄位），不需額外值欄位；② 研磨沿用既有 `grinding_level`（購物車欄位、舊訂單顯示、`translate_option` 皆已存在）→ **舊訂單零相容成本**；③ 唯讀組只顯示、不送值（`extra_options`/購物車 dedupe/`CartItem.unique_together` 皆不受影響）；④ `get_option_label/get_option_value_label/get_option_icon` 改用 `ALL_OPTION_GROUPS`（咖啡 16＋豆 3）合併查找 → **既有咖啡呼叫端零改動**。實作：`eshop/models/option_definitions.py`（定義＋`get_bean_option_groups`（依排序＋帶 `display_value`＋唯讀無值不渲染）＋`sort_option_keys_for_bean`）；`BeanItem` 新增 6 欄位（migration 0071）；資料 migration 0072 正規化產地（`哥倫比亞`→`CO`；「埃塞俄比亞 拼配 巴西」→`OT`＋原文保留 `origin_custom`；6 款豆：1 筆 CO、2 筆 OT、3 筆空）；`eshop/admin.py` 泛化（`OptionGroupConfigWidget(groups=…)`、`OptionGroupsConfigFormMixin`、`BeanOptionGroupsConfigForm`、BeanAdmin fieldsets 重排＋移除舊「研磨选项」獨立欄位組）；`views.Bean.get` 帶 `option_groups`；`bean.html` 改資料驅動（可選組＝`.bc-option-btn`＋hidden `option_<key>`、唯讀組＝`.bc-option-readonly-value` 金色晶片）＋移除 block-23 重複產地行與硬寫研磨組；`cart/views.add_to_cart`（只收可選組、相容裸 key、研磨同步舊欄位）＋`cart_count`（依豆排序＋`extra_options_icons/labels`）；`order.py`（`translate_option` 合併映射、bean 補 `extra_options_cn/icons/labels`、研磨已在 extra_options 時移除 `grinding_level_cn` 避免各顯示端重複、`options_display` 改與咖啡相同的 `bc-options-row`）；顯示端移除硬寫（`order_confirm.html`×2、`checkout.html`、`bc-slideout-cart.js` 豆分支改用 API 標籤/圖示）；員工端渲染器無需修改。**驗證**：`manage.py check` 0 issues、`migrate` 0071/0072 OK；`docs/verify/verify_bean_group_order.py` **48/48**（資料層／Admin 渲染與寫回／咖啡端 16 組零回歸／詳情頁唯讀-可選渲染與排序／購物車只收可選組／cart_count／新舊訂單顯示／模板編譯）；headless 4 尺寸實測（豆1 產地=哥倫比亞、豆3=埃塞俄比亞 拼配 巴西、豆6 空產地不渲染、咖啡4 三組排序不變）。**踩雷修正**：唯讀晶片原本變白且圖示飛到行尾 → CDP `CSS.getMatchedStylesForNode` 找到 `.product-details span{color:#eee}`（0,1,1）與 `.icon{position:absolute}` 蓋掉單一 class（0,1,0）→ 選擇器改 0,3,0 並覆寫 `position: static`（實測 `rgb(196,155,99)` ✓）；版號 bc-components.css **?v=20260921a**。

